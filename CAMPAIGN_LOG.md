2026-07-05 02:23:44 newsletter-digest: Completed visual verification + fixes for the monthly-digest email template (commit 8a9fe45). Regenerated the July 2026 draft, verified in admin UI (desktop+mobile): real logo header, DON'T MISS highlights bar, three separated segments (announcements/events/feed) with Read-more + See-all links, style controls round-trip AND persist (accent gold, section toggle -> settings_json + re-rendered content_html). Raw email is client-safe (inline CSS only, no style/link/webfonts, 600px table, absolute URLs). FIXED: feed Read-more links were bare relative slugs (href="plexus") -> now normalized to absolute portal deep links via digestFeedUrl() in both model + render paths. HARDENED: logo img now degrades to a styled Med&X wordmark via inline-styled alt (served /assets/logo.png is WEBP-mislabeled-as-PNG -> breaks Outlook; root fix logged to cross-portal todo). node --check OK, schema-sync exit 0.
2026-07-05 02:24:06 critical-bugs+nav cluster: fixed idle onboarding tooltip swallowing topbar clicks (Projects/Network/Talk Library), hero photo first-paint race (preload+DCL paint+placeholder), widened hub/network/up containers 980/1200->1320px, added Website topbar link (new tab, noopener), self-hosted jsQR to kill rotting SRI console error
2026-07-05 02:47:12 overlay-escape+action-center: FIXED the Tech Dashboard password gate trap (was a full-screen fixed overlay with only an Unlock button -> owner had to refresh). Now closes 3 ways: X button (top-right), Cancel-and-go-back link, Esc, and backdrop click -> TechApp.closeGate() returns to dashboard. Audited every admin overlay: added a generic delegated backdrop-click close for ALL .modal-overlay dialogs (many lacked the inline handler), extended the global Esc handler to also cover settingsOverlay + plannerOverlay + ad-hoc media lightbox, added click-outside to settingsOverlay, and gave the PRApp.viewMedia lightbox an X + Esc (via .ac-lightbox marker). Verified headlessly: tech gate X/Esc/click-outside all return to dashboard, settings X/Esc/click-outside, task modal click-outside, planner Esc. Left intentionally: onboarding first-run gate (completing = the exit), member-picker dropdown + cmd palette (transient, Esc/click-outside already), QR modal (already 3-way). RESTRUCTURED the Action Center home: was a flat wall of ~9 task cards; now a calm 'Start here - most urgent' spotlight (top 3-5 by urgency, max 2 per area) + collapsed per-area group rows ('Plexus 1 open', 'PR & Media 2 open', urgent areas get a crimson count badge) each expandable, plus a 'Show all tasks' escape hatch that opens/collapses everything. Grouping is client-side over the existing nag_items API (no backend/schema change); every item stays reachable, nothing deleted; expanded state survives the periodic re-render. Verified desktop+mobile, group expand, show-all/collapse, no console errors. schema-sync exit 0.
2026-07-05 02:52:09 onboarding-wizard: polished signup/onboarding wizard — crossfade welcome->step transition, desktop 2-col profile form, added Building Bridges (4th project) + change-later note, replaced dragging feature carousel with condensed 2x2 capabilities grid (fixed 'three programs'->'four projects'), restyled +50 finish modal to premium cream card w/ readable contrast + crimson CTA
2026-07-05 03:09:20 overlay-close-fix: Fixed inline-display poison in generic .modal-overlay backdrop handler (37716) + App.closeModal (18282) — both now clear inline display instead of forcing display:none. All 9 flagged modals reopen after backdrop-click and X-close (verified headless, computed styles + screenshots). Schema sync OK.
2026-07-05 03:20:20 member-home cluster: differentiated 5 project hub cards (per-project accent strip + tinted icon, distinct FA icons), centered the value-links strip (wrap+center desktop / scroll mobile), added a real dashboard snapshot (points/registrations/following/member-since) under Your Dashboard to kill the orphan heading, fixed + upgraded Latest-from-MedX feed (new /api/feed/home merges feed_items + audience-gated announcements + community forum posts + a What-is-happening summary via shared aiDraft with a hand-written composed fallback, never [Draft]; MemberFeed now owns #newsCompactList and NewsWidget defers to it), and replaced the duplicate bottom program trio with a Grow-your-network band (3 teaser cards -> Network tab). Verified logged-in desktop+mobile, zero console errors, schema-sync exit 0.
2026-07-05 04:09:37 plexus-conference-page: Fixed 10 Plexus overview items — face-centered keynote portraits (per-speaker zoom/focus), institution logo lockups + click-to-open elegant bio popups (aiDraft-ready curated bios), prominent Program-at-a-glance section + See-the-full-program CTA, widened key-dates/gala/connect/info-grid/my-schedule to hero full-width, filled Moments gallery with 6 real local Plexus photos (CC4 library was iCloud-evicted -> used accessible MedX_2027_Fundraising photos), moved My Schedule near top with register CTA on the module, added Venue-announcement-coming-soon slot (id-hooked for hotel name), promoted Get-updates toggle onto the hero below countdown (wired to MedXNotify, no dup cream row), fixed countdown-label contrast, restyled View-Schedule/I'm-Interested for readable contrast. Verified desktop+mobile, 0 console errors, schema-sync exit 0.
2026-07-05 04:18:25 gala-command-center: Built the Gala command center under Plexus (its own dashboard view). Added Meals tab (per-option aggregate + editable menu-options manager gala_menu_options + off-menu follow-up flags that never mutate guest rows), Institutions tab (grouped-by-institution + AI who-is-coming brief via aiDraft with clean deterministic fallback + notable-guest detection), Program tab (reorderable/editable schedule + premium pictured Notify-attendees email with logo+gala photo, approval-gated, with schedule-for-later), and per-guest + bulk personalized messaging (portal message for members via user_notifications targeted 1:1, email for non-members, all staged pending_approval in the single outbox, drafts via aiDraft). Portal messages deliver instantly on approval (avoids the user-portal dev drainer race; prod uses admin drainer only). Guest list gains a Meal column + Message action. Backend: 8 new admin endpoints + general scheduled_for on outbox approve. Seeded 12 demo guests. Verified desktop+mobile screenshots for every item, end-to-end queue→approve→portal/email delivery, remove-option flag flow, and scheduling. node --check + check-schema-sync.sh exit 0.
2026-07-05 04:31:17 plexus-verify: PASS all 10 items (portraits/program/width/gallery/schedule/logos+bios/venue/notify/countdown/buttons) verified desktop 1440 + mobile 390, console clean, node --check OK, schema-sync 0, no frozen code touched
2026-07-05 04:41:46 gala-command-center-VERIFY: items 1-5 independently re-verified headlessly (desktop 1440 + mobile 390, E2E queue→approve→portal/email, UI remove-option flag round-trip, program reorder persistence, premium email render, mock send boundary, schema/frozen checks clean). ONE reproducible bug: schedule-for-later is ignored for member portal-channel recipients — portal notification delivers at approval time (demo: batch gala-msg-mr7ix0pg-yxkzn scheduled 10:22:50Z, delivered 08:22:50Z) while email legs correctly wait. Minor: mobile program-editor Title input ~12px sliver; outbox UI has no approve-time schedule control for non-gala batches (API-only). Verifier artifacts: 3 VERIFY-* portal notes to test user Maria + 1 scheduled mock email to demo guest (fires 10:22Z, mock), 6 program emails approved+mock-sent.
2026-07-05 04:58:19 gala-command-center-FIX: portal-channel schedule-for-later honored at approve (drainer delivers on time, batch stays together), mobile program editor Title readable via responsive .gala-program-row grid, shared OutboxApproveUI schedule-at-approval picker added to seat-confirm + post-event outbox surfaces. Crossportal todo logged: user-portal dev drainer lacks portal branch and shares the dev DB.
2026-07-05 05:11:08 gala-evening-page: Made the Gala Evening page premium — hero countdown-label contrast + frosted readable secondary button ('View the Evening Program'->schedule), promoted Get-updates toggle onto the hero (data-mx-notify=gala suppresses the cream dup row), added a networking/bridge-building feature band with a real warm-lights photo ('Where Croatian and international biomedicine meet'), re-themed the 4 highlight cards to the felt highlights (World-Class Speakers / Bridge-Building / Live Performances / Intimate by Design), added a designed 'Featured performers — to be announced' placeholder slot (overview + speakers), reworked About copy with bridge-building + elegant limited-seating language (no fake counters), added a 'The evening at a glance' program affordance -> full schedule, and replaced the 4 generic Unsplash 'Moments from Previous Galas' with 6 real Med&X gala photos (3x2). Keynote portraits already face-centered avatars (no clip). CC4 photo library was inaccessible (persistent EINTR/iCloud eviction) so used the existing downscaled real MedX gala photos in assets/photos. Presentation-only; register/pricing/QR/Stripe untouched. Verified desktop 1440 + mobile 390 across overview/speakers/register, 0 console errors, schema-sync exit 0.
2026-07-05 05:17:38 gala-command-center VERIFY: PASS — all 5 items verified headlessly (summary/guest-list/meals+menu-manager/institutions+brief/program+notify+schedule). Anti-corruption menu-delete flags off-menu guests without touching rows (DB confirmed). Member->portal / non-member->email routing + approval-outbox staging + schedule-for-later (portal hold) verified via API+DB. node --check OK, schema-sync exit 0, frozen payment/registration/QR untouched, user-portal diff belongs to another lane. Non-blocking: leftover VERIFY-A/B/C implementer test artifacts in shared dev DB; pre-existing caterer-headcount strip counts fish as meat (Meals-tab aggregate is correct).
2026-07-05 05:40:44 gala-fix: mobile 'Featured performers' slot now stacks (flex-column, body 276px vs broken 43px/746px-tall) in both overview+speakers tabs; Moments gallery cut to 4-tile 2x2 of real gala-evening photos (dropped keynote+panel conference shots)
2026-07-05 05:47:41 admin-IA+outlook: Member Ops is now the single member-experience umbrella (Portal editor/Home screen/Accelerator/Tasks and rewards/Message members/Messages); Message members + Messages nav items folded under it; Content studio member-portal card now signposts to Member Ops (one editor surface); breadcrumbs re-parented; added Email (Outlook) tab in Messages with dev mock inbox + aiDraft replies staged to the approval outbox. Verified desktop+mobile, no console errors.
2026-07-05 06:25:20 content-studio-rebuild: Rebuilt Content studio around ACTUAL content creation. Landing now leads with three creation tools (Guided wizard, Slideshow video, Graphic & PDF); website + member-portal editors demoted to a 'Manage published text' management strip (per A3 IA) but fully preserved. NEW backend (admin-only, after SCHEMA-MIRROR:END): content_studio_assets table + uploads/content-studio dir + 4 endpoints — /api/admin/content/converse (short conversational wizard reusing the planner pattern; deterministic brief w/ live facts + library photos + aiDraft caption, clean fallback never raw [Draft]), /content/asset (raw octet-stream upload bypassing the 100kb JSON limit -> hosted /uploads url), /content/assets (recent gallery), /content/schedule (pr_content_calendar approved + mock-safe Publer + pr_posts -> shows in the SAME PR review flow). Frontend ContentCreator module: canvas graphic templates (announcement/countdown/speaker/quote) with real logo chip + gold eyebrow + Fraunces headline + scrim -> PNG + print-to-PDF; client-side slideshow composer (Ken Burns pan/zoom, text overlays, aspect presets square/story/landscape, optional add-your-own-music mixed via AudioContext — NO CC0 track bundled, noted in UI) -> downloadable VP9 WebM via MediaRecorder; conversational refine (shorter/longer/other pictures/no music/make it wide/gold/headline:). Every piece downloads AND stages to the media schedule. VERIFIED headless: wizard end-to-end -> composer; PNG 3.79MB; 3s slideshow -> valid VP9 WebM 1.08MB (EBML magic + V_VP9 + frame clusters); schedule -> calendar+pr_posts+asset rows; desktop 1440 + mobile 390 composer + landing screenshots all read; website map still 6 cards. FIXED a latent bug: pr_posts INSERT dropped the optional status column (some shared DBs never got the guarded ALTER) so the Social-posts row now persists. Only console error is an offline gstatic Fraunces woff2 ERR_FAILED (loads in a real browser; canvas falls back to Georgia). node --check OK, schema-sync exit 0, SCHEMA-MIRROR untouched, no frozen payment/QR/registration code touched, test artifacts cleaned.
2026-07-05 06:26:43 network+talk-library: Redesigned Network discover (hero + full-width search, added institution filter, New/Featured members strip, cream/ink Fraunces monograms w/ photo fallback, 12-per-page Load-more instead of endless wall) and Talk Library (grouped premium sections by edition/theme, duration pills, real embeddable YouTube sample talks badged 'Sample - replaced by Plexus recordings', Plexus 2026 empty-state). Backend: guarded ALTERs talks.duration+is_sample (outside mirror) + 5 sample seeds + GET update. Verified desktop+mobile logged-in, scripted search/filter/load-more + talk pill regroup, 0 console errors, node --check + schema-sync 0.
2026-07-05 07:04:58 combo-links+newsletter cluster: (1) EVENT-COMBO INVITE LINKS — Plexus area generator card (any mix of Conference/Gala/Bridges/Donor Night + optional audience label) -> NEW public premium page GET /e/:token on the admin backend (combo_invite_links table, outside mirror); registration reuses the FROZEN entry points (/plexus/:token chooser via an auto-created registration_links row offering exactly the chosen components, /donor-night for donor); existing invite links byte-identical (verified: /invite route regions == HEAD, live CA invite page screenshot clean). Croatians Abroad section got the one-line invitee-segment explainer. (2) NEWSLETTER AREA under PR — Compose-newsletter flow (subject/message/highlight bar/auto sections/audience checkboxes with live counts) builds a pr_newsletters draft with the upgraded digest template (intro support added additively to renderDigestHtml), reviews/styles in the DigestApp modal (retitled for newsletters), stages via POST /api/admin/pr-newsletters/:id/stage into the approval outbox. (3) INTEREST SEGMENTS + DEDUPE — public signup page GET /newsletter with interest pills -> pr_subscribers upsert + newsletter_interests (email-keyed, mirror-free); 4 new subs_* segments; resolveSegmentsUnion dedupes across segments (PROVEN: overlap subscriber in 2 selected segments got exactly 1 pending_approval outbox row; batch visible in /api/admin/outbox). (4) buildEmailTemplate footer now carries a Sign-up-for-our-newsletter link globally (digest re-render verified unregressed). BONUS FIX: PUT /api/pr/newsletters no longer resets template/wipes content_json on raw-editor saves. Verified desktop 1440 + mobile 390 (combo page, signup page+success, Plexus card, newsletters tab, composer w/ live dedupe note, review modal); console clean (only known offline CDN/logo fails in digest iframe); node --check OK; schema-sync 0; test artifacts cleaned.
2026-07-05 07:08:06 MyMedX/purchases/QR/landing/verify: premium MyMedX hero (card+points+member-since), past purchases w/ refund+ask via inbox, resilient membership-card download + QR check-in line, read-only scanner enrich endpoint (+crossportal todo), elevated splash landing, first-class email-verify pending screen w/ dev link
2026-07-05 07:35:14 mymedx-fix: added member photo/monogram to MyMedX hero + membership card (photo_url via /api/auth/me, initials fallback) and fixed regConfirmEmail wrap on mobile; verified desk+mob, monogram+photo states, card download intact
2026-07-05 07:55:37 network-resources-assistant: Resources restructured into Presentations/Brochures/Handbooks+runbooks (distinct card treatments). My Network CSV/XLSX import (robust CSV parser, mapping preview, dedupe by email -> added/merged/skipped). My Network 'Compose with AI' outreach (planner: intent -> personalized per-contact drafts via aiDraft w/ clean fallbacks, optional gala brochure + link, staged to approval outbox, never sends). Admin assistant no-key gap closed: deterministic layer wired into existing FAB (data answers + contact search + draft reminder to unpaid -> outbox). Added a general Approval Outbox viewer in Email Registrants. Backend node --check OK, schema-sync exit 0, no console errors.
2026-07-05 07:58:38 MyMedX/purchases/QR/landing/verify VERIFY: PASS all 6 items (hero card+points+photo/monogram E2E, purchases refund+ask filed to direct_messages E2E w/ seeded reg then cleaned, card PNG 1600x1000 QR+check-in line downloaded, enrich endpoint read-only+admin-gated w/ frozen QR/verify byte-untouched, landing elevation, signup pending screen w/ dev link confirmed account). Minor notes: mymedx shell 1120 vs home 1320, mobile help-FAB grazes download btn, dev-DB tester ships w/ no purchases so section shows empty state OOTB.

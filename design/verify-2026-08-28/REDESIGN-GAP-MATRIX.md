# REDESIGN GAP MATRIX — Med&X member portal (+ admin destinations) · 2026-08-28

**Purpose.** The build contract for the redesign handoff (`/Users/alen/Downloads/uploads/export/medx-member-portal-final/README.md` — screens table, Product Rules, implementation notes 1–22, the "Button & control wiring map (production)", and the "Admin review round" section). One row per control or behaviour. Every line citation was checked with `grep` at `main @ 8b7ba23` (live member = `189ac8d` = same `user-portal/` tree).

**Citation tokens:** `U:NNNN` = `user-portal/backend/server.js` · `A:NNNN` = `admin-portal/backend/server.js` · `IX:` = `user-portal/frontend/index.html` · `P9:` = `user-portal/frontend/assets/app.part9.js` (`P1…P14` = other parts) · `FW:` = `forum-wing.html` · `SW:` = `user-portal/frontend/sw.js` · `AIX:` = `admin-portal/frontend/index.html`. Auth labels: **member JWT** (`auth` U:6083), **member JWT + is_admin** (`adminOnly` U:6142), **optional JWT** (`optionalAuth` U:6116), **public**, **signed-token**.

**Tree note (added 19:00 EDT).** Nine commits landed on the checked-out branch `redesign/member-portal` (`main` itself is still `8b7ba23`; the branch is 9 commits ahead of `origin/main`) between 18:20 and 18:54 EDT while this document was being written (`45fb3ea` … `7e5f5dd`: staging launcher, design-handoff snapshots, `design-diff.js`, `user-portal/frontend-v2/`), not yet pushed to `origin/main` (local only) at the time of writing. The only cited file they touch is `user-portal/backend/server.js`: `52dc4cd` adds three lines inside the CORS block (`CORS_ORIGIN` now appended additively, U:593–595), so every member route below line 592 sits 3 lines lower than in the three 2026-08-28 audit files. **Every citation in this document was re-verified against the working tree at HEAD `7e5f5dd`.** The live member service still ran `189ac8d` at 18:35 EDT (service-worker stamp), i.e. it does not include `52dc4cd`.

**Status vocabulary.** **EXISTS** = backend + data + frontend logic exist; restyle and re-wire only. **PARTIAL** = something real exists but a named piece is missing (the cell says what). **MISSING** = nothing usable exists; the cell says what to build (route / table / job).

**Cross-cutting facts that shape many rows:** the Settings "Save changes" never calls the backend (`// TODO: Send to backend API` P9:23906) and photo upload is preview-only (P9:23942–23943); profile-completion % is computed from `localStorage.medx_user_profile` (P9:48752–48765); the Forum invitation code is validated against hard-coded demo codes (P9:15017–15020); the Network directory merges a 16-person seed list (P9:16904–16920) with `/api/networking/discover`; Bridges "past events" are hard-coded (P9:18515–18560); all transactional email is dropped today (no `BREVO_API_KEY`, U:126–128); multipart uploads 503 in prod without `CLOUDINARY_URL` (U:5540–5547).

---

## A. Global chrome (Portal Chrome.dc.html) — top bar, stats strip, banner, drawer

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Chrome | SEARCH → global search overlay (members, events, news) | one field, typed results, same agent API as the admin header search | `GET /api/member/search?q=` U:13470 (member JWT) → `{events, members, talks, mine}`; members come **only** from `forum_members` (U:13537–13542) | `GlobalSearch` P3:26–126 (debounced `_run` P3:60–66, overlay `#upSearchOverlay` IX:2506) | **PARTIAL** — no all-members or news group; no intent/agent | extend U:13470 with `users` (public profiles) + `feed_items`; the admin agent `POST /api/admin/assistant` A:36765 is admin-only on the admin origin — a member-safe intent endpoint does not exist (see A-15) |
| Chrome | ALERTS dropdown + live unread dot | list + unread count from the server | `GET /api/user-notifications` U:23057 (returns `unreadCount`), `PUT …/:id/read` U:23081, `PUT …/mark-all-read` U:23088, `GET /api/announcements` U:23214, `GET /api/bell-feed` U:23102, admin messages `GET /api/user/admin-messages` U:28050 | `Dashboard.toggleNotifications` P9:6839, `markAllNotificationsRead` P9:6951, `handleNotificationClick` P9:6970; `NotificationSystem.loadNotifications` P9:52719, `render` P9:52939 | **EXISTS** | merge the three sources exactly as `NotificationSystem` does; the dot = `unreadCount` + unread admin messages (`is_read` U:9275) |
| Chrome | EN · HR switch (visible; HR ships when translations exist) | persist locale per device and per profile | `GET /api/me/locale` U:13699, `PATCH /api/me {locale}` U:13708 (`users.locale`) | `MedXI18n` P1:1961 (keys `medx_locale` P1:6, `persistServer` P1:1877, `hydrateFromServer` P1:1932; 1,617 HR strings in `DICT` P1:10; 1,363 `data-i18n` hooks in IX) | **EXISTS** | keep the `medx:localechange` event (P5:42) that re-renders widgets |
| Chrome | RESEND LINK (email-confirm banner; also Profile, My Med&X) | resend the confirmation email + "sent" toast | `POST /api/auth/request-verification` U:11675 (public, `authLimiter`); legacy `POST /api/resend-verification` U:11281; confirm `GET /api/auth/verify?token=` U:11704 → `/?verified=…` | `UserPortal.resendVerification` P9:3842 (buttons `#regResendBtn`, `#resendVerificationBtn`, `#upVerifyResendBtn` IX:2524); boot handling of `?verified=` P9:3403–3434 | **EXISTS** (email delivery blocked until `BREVO_API_KEY` is set) | banner hidden when `email_verified=1` (`GET /api/auth/me` U:11906) |
| Chrome | Member-stats strip | live counts | `GET /api/member/meta` U:12971, `GET /api/rewards/summary` U:26360, `GET /api/my/events` U:13058, `GET /api/networking/connections` U:26499 | `MedXDashSnapshot` IX:2901–2906 | **EXISTS** | choose the four numbers; all are server reads |
| Chrome | Drawer: Certificates → My Med&X record | open the record with certificates | `GET /api/member/record` U:13109 (`certificates[]` U:13131–13139) | `MyMedXPortal.renderRecord` P9:1107, `downloadCertificate` P9:1245 (HTML print window + `/api/org/signature` U:12069) | **EXISTS** | — |
| Chrome | Drawer: Forum eligibility → Biomedical Forum | route to Forum section | `GET /api/forum/me` U:16927 | `UserPortal.showSection('forum')` P9:4413 | **EXISTS** | — |
| Chrome | Drawer: Mentorship page (feature unchanged) | mentor/mentee profiles + requests | `GET/PUT /api/mentorship/profile` U:14225/U:14231, `GET /api/mentorship/mentors` U:14252, `POST/GET /api/mentorship/requests` U:14282/U:14305, `PUT …/:id` U:14330 | `Mentorship` P9:47413 (`load` P9:47419, `saveProfile` P9:47466, `respond` P9:47624) | **EXISTS** | restyle only |
| Chrome | Drawer: Opportunity board (feature unchanged) | list + submit | `GET /api/opportunities?kind=` U:13980, `POST /api/opportunities` U:13991 (pending until admin approves A:12475 / U:14157) | `OpportunityBoard` P9:47251 (`load` P9:47262, `submit` P9:47384) | **EXISTS** | — |
| Chrome | Identity block (avatar/name) → My Med&X | navigate | `GET /api/auth/me` U:11906 (`photo_url`) | `MyMedXPortal.loadPhoto` P9:2250; `showSection('mymedx')` | **EXISTS** | — |
| Chrome | Drawer active state, hash routing, back/forward | `#<section>` deep links survive (emails link `#gala`, `#mymedx`) | — | `UserPortal.showSection` P9:4413–4470 (`#up-section-<id>` ids IX:2528…11444), `popstate` P9:3541–3549, `MedXRail.set` P2:23 | **EXISTS** | keep the 13 section ids (`talks` may redirect to `dashboard`) |
| Chrome | Talk Library removed (note 16) | no menu entry, no page | `GET /api/talks` U:14010 stays server-side | `TalkLibrary` P9:47831 — delete; `#up-section-talks` IX:9872 — delete | **EXISTS** (removal) | also drop `talks` from `MedXBottomNav.MAP` IX:1288 |
| Chrome | No abstract submissions anywhere (note 6) | remove UI | `/api/plexus/abstracts*` U:21612–21693, `/api/abstracts*` U:13719–13744 stay | `PlexusPortal.submitAbstract` P9:10090 — delete; upload `/api/upload/abstracts` P9:10154 | **EXISTS** (removal) | keep the admin routes; hide `data-tab="abstract"` |
| Chrome | Time-of-day greeting + occasional Croatian greeting (note 17) | morning/afternoon/evening; ~15 % of sessions in Croatian | — | greeting by clock P4:21 (`home.greetMorning|Afternoon|Evening`); `#upWelcomeHeader` IX:2547 | **PARTIAL** — Croatian variant missing | client-only: add the three HR strings (`Dobro jutro / Dobar dan / Dobra večer`) with a `Math.random() < 0.15` per session |

## B. Home (Med&X Home.dc.html)

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Home | Rotating hero photos (admin-uploaded set, 6 s) | rotate admin images | `GET /api/portal-content/published/featured` U:28097 (`portal_content.image_url`, admin `POST /api/portal-content` A:33888, publish A:33923) | `FeaturedCarousel.loadFromAPI` P9:5561 → `#featuredCarouselTrack` IX:2914 | **EXISTS** | image upload path = admin `POST /api/upload/:type` A:19090 (needs Cloudinary in prod) |
| Home | Next-event panel: name, dates, venue, status label, countdown target, CTA labels (admin-editable) | one server-driven object | pieces exist: `GET /api/me/next-event` U:23170 (active conference + my registration), `GET /api/plexus/conference` U:19707 (dates, venue, tickets), `GET /api/project-status` U:14065 (`status_label`, `detail_line`, `cta_label`, `cta_target` — admin `PUT /api/admin/project-status/:key` A:11770), `GET /api/gala/settings` U:26994, `GET /api/bridges/events` U:16423; `project_settings` (dates/venue per project, admin A:17991) has **no member read** (U:18983 is admin-only) | `MedXNextEvent` P4:10–49 (`#upNextEvent` IX:2683), `CountdownWidget` P9:50282, hard-coded `MEDX_DATES` P9:7 | **PARTIAL** — no single "next event" aggregator; countdown target hard-coded | build `GET /api/home/next-event` (picks the soonest of conference/gala/bridges for this member, returns `{name, start, end, venue, status_label, cta_label, cta_target, countdown_at}` from `conferences`+`gala_settings`+`bridges_events`+`project_status`); drop `MEDX_DATES` |
| Home | Profile-completion nudge ↔ full-width countdown swap at 100 % | server-driven % | `GET /api/member/profile-nudge` U:13612 (boolean: photo/institution missing), dismiss U:13628 (`dashboard_preferences`) | `MedXProfileNudge` P5:7–43 (`#upProfileNudge` IX:2700); `ProfileCompleteness.calculatePercent` P9:48764 (localStorage) | **MISSING** — server-side completion % | build `GET /api/me/profile-completion` → `{percent, missing:[…]}` over `users.{photo_url,bio,institution,country}` + `networking_profiles.research_interests` + `user_profiles.title`; used by Home nudge, Profile checklist and the Getting-started card |
| Home | Project cards (statuses like PRE-REGISTRATION OPEN / BY INVITATION, dates, metas) | admin-editable | `GET /api/project-status` U:14065 ← `project_status` (A:11770) | `MedXProjectHub` P6:7–83 (`fetch /api/project-status` P6:52, follow toggle P6:79) | **EXISTS** | dates/venue per card: add `project_settings` fields to `/api/project-status` (one JOIN) |
| Home | Latest from Med&X (news items) — READ / SEE ALL | article page + archive | `GET /api/feed/home` U:13895 (20 items + announcements + forum posts + summary), `GET /api/feed` U:13858; rows = `feed_items` (type, title, body, link_url, link_label, image_url, posted_at, published U:9402); admin `POST /api/admin/feed-items` A:12397 | `MemberFeed.load` P9:47095 (`#newsCompactList` IX:3020), `NewsWidget` P9:46944 | **PARTIAL** — no single-article route and no archive pagination | add `GET /api/feed/:id` and `?page=` on U:13858; render `body` as the article (or open `link_url`) |
| Home | From the Forum teaser (note 22) | latest forum post between news and network | `GET /api/forum/posts?limit=1` U:17228 (member JWT; `moderation_status='approved'`) | — | **PARTIAL** — feed exists, "kind/tag/published" composer fields missing (see F-6) | same endpoint feeds Home, Forum page and Network |
| Home | Key dates list (names + dates, admin-editable) + ADD → `.ics` | list + per-date calendar file | `GET /api/plexus/settings` U:28105 (`key_dates` JSON, admin `PUT /api/admin/plexus/settings` A:34551), `GET /api/accelerator/key-dates` U:23255 (admin A:13535/A:13561/A:13589); event `.ics` `GET /calendar/:file` U:3191 (`calendarEventsFor` U:3065: slugs `plexus`, `building-bridges`, …), feed `GET /calendar/medx-events.ics` U:3150 | `AcceleratorPortal.loadPublicKeyDates` P9:12401; `.ics` links in `renderMyEvents` P9:906 | **PARTIAL** — no cross-project key-dates endpoint; no per-key-date `.ics` | build `GET /api/key-dates` (union of `plexus_settings.key_dates`, `accelerator_key_dates`, gala/bridges dates) returning an `ics_url` per row (`/calendar/key/:id.ics`) |
| Home | CHECK IN → wallet QR | jump to My Med&X QR | `GET /api/my/events` U:13058 (`ticket` URL `/qr/<id>.png` U:4077) | `showSection('mymedx')` | **EXISTS** | — |
| Home | SUBSCRIBE (newsletter compact rail card) with topic chips | subscribe the member's email to any-or-all topics (All, Plexus, Gala, Accelerator, Bridges, Forum); manageable from Profile | in-portal follows only: `GET/POST /api/notify-topics` U:14079/U:14084 (`notify_topics`, no email); email opt-outs `email_optouts` (U:1834/U:1840/U:1860); admin-side `pr_subscribers` (`POST /api/public/newsletter/subscribe` A:23777 — **CORS allows only website origins** A:868–873) and `newsletter_interests` (A:23576/A:23593, admin-only) | `MedXNotify.toggle` P7:19 | **MISSING** — member newsletter endpoint | build `GET/PUT /api/me/newsletter` on the member origin writing `pr_subscribers` + `newsletter_interests` (topic keys `all, plexus, gala, accelerator, bridges, forum`), and `email_optouts` scopes for Unsubscribe |
| Home | GETTING STARTED card — rows disappear as server state completes; dismissal persists per user | server state | `email_verified` via `GET /api/auth/me` U:11906; profile % (B-3); dismissal pattern `dashboard_preferences` (`section='home', card_id=…`) U:13628–13640, founder welcome U:13648/U:13659 | `FounderWelcome` P9:3232, `HomepageNotifications.dismiss` P9:48664 (localStorage) | **PARTIAL** — generic dismissal route missing | add `POST /api/me/dismiss {card_id}` (+ read in `/api/auth/me`) reusing `dashboard_preferences` |
| Home | Member card teaser / QR | show the member QR | `GET /api/member/verify-link` U:12989 (badge token → `/verify/:token` U:2720) | `MemberLinkCard` P9:29 (`_loadVisibility` P9:36 ← `GET /api/member-card-visibility` U:1771), `MedxCard` P9:2649 | **EXISTS** | — |

## C. Plexus — overview · Program & Speakers · Zagreb · My Plexus

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Plexus overview | REGISTER NOW / REGISTER — FREE / RESERVE A SEAT / RSVP → the ONE external form, preselecting conference and/or Gala | open `/plexus` with the right component pre-ticked | `GET /plexus` U:1295 (server-rendered wizard, components `conference|bridges|gala` U:1307; reads only `?t=`/`:token` U:1298 and prefill `fn/ln/email/inst` U:1392–1396); submit `POST /api/plexus/register` U:19754 (optional JWT; creates the user U:19763–19773) | in-SPA fallback `PlexusPortal.submitRegistration` P9:9537, `redirectToStripeCheckout` P9:9717 | **PARTIAL** — no preselect parameter | add `?pick=conference,gala` (and read `mxt`) in U:1295; keep `/plexus/:token` |
| Plexus overview | Free conference facts (Dec 4–5, Novinarski dom, cap 100, 9th year) | from admin data | `GET /api/plexus/conference` U:19707 (`conferences` + `ticket_types`), `GET /api/plexus/settings` U:28105, `GET /api/public/site` U:11946 | `PlexusPortal.loadPlexusSettings` P9:8775 | **EXISTS** | "cap 100" = `conferences.capacity`; edit via `PUT /api/admin/conferences/:id` A:11120 |
| Plexus overview | I'M INTERESTED → follow + notify | label flips, updates on | `POST /api/notify-topics {project:'plexus', on}` U:14084 | `MedXNotify.toggle` P7:19, `_reflect` P7:23 | **EXISTS** | — |
| Plexus overview | ADD TO CALENDAR → `.ics` | download | `GET /calendar/plexus.ics` U:3191 (live 200) | link in `renderMyEvents` P9:906 | **EXISTS** | — |
| Plexus overview | Program at a glance | published rows | `GET /api/plexus/schedule` U:21721 (published `sessions` + tracks + rooms) | `App.loadPlexusSchedule` P9:29431 (staff view) | **EXISTS** | "Program in preparation" = empty array |
| Program & Speakers | Admin-published program rows {date/time, title, note}; "in preparation" until published | rows only when `is_published=1` | `GET /api/plexus/schedule` U:21721, `GET /api/plexus/sessions` U:28127, detail U:21825; admin `POST /api/admin/plexus/sessions` A:20183, `PUT …/:id` A:20216, publish A:20250, bulk A:20267 | `PlexusLiveBoard` P14:354 (poll P14:314–318) | **EXISTS** | networking events + Gala rows live in `sessions` too (type column) |
| Program & Speakers | VIEW BIO / BIO → speaker bio modal (admin-entered) | modal | `GET /api/plexus/speakers` U:22211 (`bio`, `photo_url`, `talk_title`, `is_keynote`; published + confirmed only) | `PlexusPortal.loadSpeakersFromDB` P9:8891 | **EXISTS** | portraits = `speakers.photo_url` (admin upload A:19090); **institution logo has no column** → add `speakers.institution_logo_url` |
| Program & Speakers | ADD SESSION → add-to-my-schedule | toggle per session | `POST /api/plexus/my-schedule/:sessionId` U:21848, `DELETE` U:21858, `GET /api/plexus/my-schedule` U:21865 (`personal_schedules`) | `SessionBookmarks` P9:45067 (localStorage today) | **EXISTS** | replace the localStorage bookmarks with the routes |
| Program & Speakers | DOWNLOAD PROGRAM · PDF | PDF from admin rows | none | none | **MISSING** | build `GET /api/plexus/program.pdf` with `pdfkit` (pattern: accelerator package U:14826, fonts U:15845–15857) from `sessions` + `speakers` |
| Program & Speakers | ALL PHOTOS → per-event galleries (admin uploads) | gallery | `GET /api/plexus/photos` U:22248 reads `conference_photos` (U:7873) — **no writer in either backend** | none | **PARTIAL** — read exists, admin upload missing | add admin `POST /api/admin/plexus/photos` (multer → Cloudinary) or reuse `pr_media_assets` (A:27739) with a `public=1` flag |
| Program & Speakers | Speakers list (name, role, institution, portrait, logo, confirmed flag) | admin-driven | `speakers.is_confirmed`, `is_published` U:6231–6238; admin CRUD A:20290/A:20437/A:20844 | as above | **EXISTS** (logo column missing, see C-7) | — |
| Explore Zagreb | Travel/venue guidance | static copy + maps | none needed; maps links P9:11379 | `AdminZagrebEditor` P9:46725 (static) | **EXISTS** | content is static; optionally a `portal_content` section `zagreb` |
| My Plexus | Registration state, package, Gala add-on, QR pass slot, help | server state | `GET /api/plexus/my-registration` U:21513, `GET /api/me/next-event` U:23170 (`has_gala`, `balance_due`), `GET /api/plexus/registration/:id/invoice` U:20060 (bank details, FIRA), `GET /api/gala/my-status` U:27747, QR `/qr/<id>.png` U:4079 | `PlexusPortal` "My Pass" tab (`data-tab="mypass"` IX:6001), `handleStripeReturn` P9:9822 | **EXISTS** | — |
| My Plexus | MESSAGE (attendee rows) → Messages thread with that member | open DM | `POST /api/messages` U:26727 (`direct_messages`), attendees `GET /api/plexus/attendees` U:22031 | `NetworkingPortal.loadConversationsList` P9:17180 | **EXISTS** | attendee visibility gate `user_profiles.is_profile_public` U:22039 |
| My Plexus | Help → MESSAGE US (no mailto) | portal inbox thread to the team | `POST /api/assistant/escalate` U:12676 or `POST /api/accelerator/ask-coordinator` U:14566 (both insert `direct_messages` sender_type `user` → admin inbox `GET /api/admin/messages` A:34302) | `MedXAssistant` P13:77 (escalate) | **PARTIAL** — no generic "message the team" route with a topic | add `POST /api/messages/team {topic, body}` writing `direct_messages(receiver_type='admin', title=topic)`; 16 `mailto:` links in IX must go |
| My Plexus | Ticket transfer / refund request / visa letter / volunteer / scholarship (existing features) | keep | U:21546, U:21582, U:22131, U:22162/U:22177, U:21597 | P9:11393, P9:11423, P9:11475, P9:11445 | **EXISTS** | Gala seats are non-refundable (note 6): hide the refund form for gala rows |

## D. Gala Evening

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Gala | RESERVE YOUR SEAT / RSVP → same Render form | request a seat (pending → approve → pay) | `/plexus` gala component U:1307 (guests `pf_guests` U:1426–1431) or `POST /api/gala/register` U:26873 (optional JWT, rings admin bell U:26886–26892); approve `PUT /api/gala/registrations/:id` A:30106 (twin U:26954) → pay `/pay/gala/:token` U:27871 or `POST /api/gala/checkout-session` U:27786 | `GalaPortal.submitRegistration` P9:12123, `checkMyStatus` P9:11928, `startPayment` P9:12012 | **EXISTS** | "limited seating" = `gala_settings.capacity` U:8574 |
| Gala | Price flips EUR 150 → 175 after Sep 1 automatically (server config) | show the effective price | `effectiveGalaPrice()` U:5712–5730 (`event_components.gala` / `gala_settings.price_gala_early_bird|regular`, `early_bird_deadline` default 2026-09-01); `GET /api/gala/settings` U:26994 returns **raw** columns (`price_gala_only`, `price_bundle`) | `GalaPortal.applySettings` P9:11805–11834 reads `price_gala_only` (legacy 95) | **PARTIAL** — effective price not exposed | add `effective_price`, `next_price`, `flip_date` to U:26994 (call `effectiveGalaPrice()`); the `/plexus` page already uses it (U:1314) |
| Gala | ADD TO CALENDAR | `.ics` | `GET /calendar/:file` U:3191 — confirm a `gala` slug exists in `calendarEventsFor` U:3065 | — | **PARTIAL** | add the `gala` slug if absent |
| Gala | Performers: TBA slots flipped to names by an admin flag (`performersAnnounced`) | admin-controlled | `gala_settings.speakers_json` (parsed as `speakers` U:26997) via `PUT /api/admin/gala/settings` A:30009 | `GalaPortal.applySettings` | **PARTIAL** — no explicit flag | add `performers_json` + `performers_announced INTEGER` to `gala_settings` (both SCHEMA-MIRROR blocks) and expose in U:26994 |
| Gala | Black tie, seats, Esplanade, Dec 5 facts | admin-editable | `gala_settings` (`date`, `venue`, `dress_code`, `capacity` U:8574) | as above | **EXISTS** | — |
| Gala | Non-refundable copy; no refund UI | never show refund copy | member refund route exists for conference only U:21582; admin refund U:736 | — | **EXISTS** | — |
| Gala | My seat/table | show assignment | `GET /api/gala/my-seat` U:13026 (`gala_table_assignments`, `gala_tables`; admin A:12664/A:12698) | `MyMedXPortal._loadMemberData` P9:1008 | **EXISTS** | — |

## E. Accelerator — overview + 7-step wizard (Accelerator.dc.html, Accelerator Application.dc.html)

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Accelerator | GET NOTIFIED (hero + Your application) | notify-list capture, toast + ✓ | `POST /api/notify-topics {project:'accelerator'}` U:14084 | `MedXNotify.toggle` P7:19 | **EXISTS** for signed-in members | opening email = admin announcement to `notify_topics` followers (A:11985 with `push=1`) |
| Accelerator | Host institution cards (name, city, positions, blurb, logo) — expandable, admin-editable | list | `GET /api/accelerator/institutions` U:14529 (`accelerator_institutions`: name, short_name, city, country, description, website_url, logo_url, available_spots U:6327); `GET /api/accelerator/sites` U:14551 (`accelerator_sites`: institution, city, lab_or_clinic, mentor_line, spots, year U:9591); admin A:13657/A:13645/A:13612, A:12051/A:12067/A:12086 | `AcceleratorPortal.loadDynamicInstitutions` P9:12649, `loadSites` P9:12517 (`#axSitesGrid`) | **EXISTS** | "positions" = `available_spots`/`spots`; "blurb" = `description`/`mentor_line` |
| Accelerator | What's included / selection / team / FAQ accordion (answers admin-editable) | editable copy | `GET /api/accelerator/overview-config` U:23317 (admin A:13511); FAQ answers: none (`faq-kb.js` is the assistant KB) | `AcceleratorPortal.loadOverviewConfig` P9:12452 | **PARTIAL** — FAQ not editable | store FAQ rows in `portal_content` (`section='accelerator-faq'`, admin A:33888) and read via U:28097 |
| Accelerator | Previous cohorts panel — auto-rotates 4 fellows / 4.5 s; names/placements from admin | data | none (`feed_items` spotlight text only U:10410) | none | **MISSING** | build `accelerator_alumni` (id, name, year, placement_institution, city, photo_url, sort) + admin CRUD `/api/admin/accelerator/alumni` + public `GET /api/accelerator/alumni`; seed the published 2024–25 names |
| Accelerator | RESULTS LOOKUP + VIEW RESULTS (AX26-XXXX; distinct errors empty vs unknown) | validate + fetch | `GET /api/accelerator/results?code=` U:16393 (`accelerator_result_codes`, format `AX26-XXXX` generated at A:15841) | `AcceleratorPortal.unlockResults` P9:13756 (`#axResultsCode` IX:7668) | **EXISTS** | — |
| Accelerator | Key dates / countdown ("opens Dec 8, 2026"; no public close date) | admin-driven | `GET /api/accelerator/key-dates` U:23255, `GET /api/accelerator/countdown` U:23273, intake window `GET /api/accelerator/intake` U:16760 (admin `PUT /api/accelerator/intake/window` U:16909) | `loadPublicKeyDates` P9:12401, `fetchCountdownTarget` P9:12807 | **EXISTS** | — |
| Accelerator | PREVIEW THE APPLICATION → restyled 7-step wizard | open wizard | see next rows | — | **EXISTS** | — |
| Wizard | Steps Personal · Education · Program · Supplementary · Documents · Consent · Review, clickable stepper, Previous disabled on step 1, autosave note | reuse existing | draft `POST /api/accelerator/applications` U:14586 (create/update), my application `GET /api/accelerator/applications/my` U:14534, submit `POST …/:id/submit` U:14769 | markup IX:7281–7640 (stepper IX:7283–7313, panels `#ax-wizard-step-1…7` IX:7317+, review IX:7580–7626); `AcceleratorPortal.nextStep` P9:13308, `prevStep` P9:13327, `validateStep` P9:13340, `collectFormData` P9:13443, `updateReview` P9:13468, `submitApplication` P9:13494 | **EXISTS** | field vocabulary per README note 4; steps 2–6 keep their existing fields |
| Wizard | Application Checklist sidebar + % complete (one completion source shared with the Review list; "Application reviewed" completes only on submit) | server or single client source | `accelerator_applications.status` + document rows `GET /api/accelerator/applications/:id/documents` U:16362 | `updateChecklist` P9:13154 (`.ax-checklist` IX:7240), `saveDraft` P9:13419 / `loadDraft` P9:13426 (localStorage `ax_application_draft`) | **PARTIAL** — % is client-derived from the draft, not from the saved application | compute `completed_steps` server-side in `GET /api/accelerator/applications/my` and drive both checklist and review from it |
| Wizard | Documents step uploads | upload per doc type | `POST /api/accelerator/applications/:id/documents/:docType` U:14628 (multer → `cloudUpload` U:5570), delete U:14741, download U:16344 | `submitApplication` P9:13494 (uploads after submit) | **EXISTS** (needs `CLOUDINARY_URL` in prod, else 503 U:5540–5547) | — |
| Wizard | Processing fee (existing) | Stripe 75 EUR | `POST /api/accelerator/checkout-session` U:15060 (return `?payment=…&type=accelerator&app=` U:15100) | `AcceleratorPortal.startPayment` P9:13601 | **EXISTS** | keep only if the fee is still policy |
| Wizard | Review PDF | generate | `GET /api/accelerator/applications/:id/package` U:14824 (pdfkit) | `generateReviewPDF` P9:13823 | **EXISTS** | — |
| Accelerator | Status after submit (validity, messages, decision, ranking) | show admin decisions | `GET /api/accelerator/my-applications` U:16377 (reads `accelerator_messages` U:15200); admin validity A:13794, message A:13757, decisions A:14973, publish rankings A:15799 (**phantom `notifications` insert A:15819** — notify via `user_notifications` instead) | `loadAppStatus` P9:12552 (`#axAppStatusStrip`) | **EXISTS** (fix A:15819) | — |
| Accelerator | MESSAGE US (no "Ask the coordinators" form) | portal inbox | `POST /api/accelerator/ask-coordinator` U:14566 → `direct_messages` | `askCoordinator` P9:12597 | **EXISTS** | rename UI only; same route |

## F. Biomedical Forum

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Forum | UNLOCK REGISTRATION → invitation-code validation; success JOINS the network (stage 2) and unlocks gathering registration; works with or without an account (creates it inline) | server-validated code | codes exist: `forum_invitations.invitation_code` (created by U:18083 / U:18103, member-origin admin routes, **no email sent**); membership rows `forum_members` (U:6577); magic-link admission `forumAdmitAndInvite` A:39380–39388 → `/forum/enter?token=` U:4166; apply `POST /api/forum/apply` U:16935 (pending) | `ForumPortal.verifyInviteCode` P9:15007–15033 (**hard-coded demo codes**, `sessionStorage.medx_forum_verified`), `showMemberArea` P9:15035 | **MISSING** — redemption route | build `POST /api/forum/redeem-code {code, email?, first_name?, last_name?, password?}` (public + `authLimiter`): look up `forum_invitations` by code, create/attach the `users` row when anonymous, insert/approve `forum_members`, mark the invitation used, return a member JWT; make admin "SEND CODE" actually email (U:18083 logs only) |
| Forum | Membership is ANNUAL and renewable; terms shown in the registration form | expiry + renewal state | `forum_members.approved_at`, `membership_level` (no expiry column) | — | **PARTIAL** | add `forum_members.valid_until` (+ mirror block) and a renewal path (re-redeem or admin renew) |
| Forum | COMPLETE REGISTRATION → gathering registration (May 28–29, 2027; 3-stage indicator) | register | `GET /api/forum/events` U:17338, `POST /api/forum/events/:id/register` U:17368 (optional JWT, `registrationLimiter`, confirmation email), paid → `POST …/checkout-session` U:17483; AF26 form `POST /api/af26/register` U:18607; wing convenings `POST /api/forum/wing/convenings/:id/reserve` U:4410 | `ForumPortal.completeRegistration` P9:15151, `AF26Portal.submitRegistration` P9:16696; `Wing.reserve` FW:842 | **EXISTS** | pick one of the three registration paths for the redesign (recommend U:17368) |
| Forum | "From the Forum" feed (newest = featured, rest grid); fields kind, tag, name/role/init or title, body, publishedAt, published; unpublish hides, never deletes | admin composer → feed | `forum_posts` (post_type, tags, is_pinned, is_featured, moderation_status U:6663–6685) via `GET /api/forum/posts?type=` U:17228 (member JWT); admin create `POST /api/forum/posts` A:16509 (admins may post without membership), moderate `PUT /api/admin/forum/posts/:id` A:17183 (pinned/featured/status) | `App.loadForumPosts` P9:30925, `likeForumPost` P9:31011 | **PARTIAL** — composer semantics missing | map `kind` → `post_type` (`spotlight|news|note`), `published` → `moderation_status`, featured → `is_featured`; add `published_at`; expose a public-to-members read used by Home/Forum/Network |
| Forum | Evening schedule + speakers | show | `GET /api/forum/events/:eventId/schedule` U:17577 (`forum_event_schedule`, admin A:17118), AF26 speakers `forum_event_speakers` (member-origin admin routes U:18340) | `App.loadAF26Schedule` P9:33549 | **EXISTS** | — |
| Forum | Forum members surface in Network year-round | directory | `GET /api/forum/members` U:16984 (paginated, filters), wing directory U:4352 | `App.loadForumMembers` P9:31784 | **EXISTS** | — |
| Forum | Enter-code screen in Auth (Auth.dc.html) | same redemption | see F-1 | — | **MISSING** (same route) | — |
| Forum | Public interest form (website) → candidate pipeline | already wired | `POST /api/public/forum-consideration` U:4332 → admin A:39410/A:39425 | — | **EXISTS** | — |

## G. Building Bridges in Biomedicine

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Bridges | Next event (Boston, Sep 18–21 2026, date/venue announced later) + countdown + photo | admin-driven | `GET /api/bridges/events` U:16423 (`bridges_events`: name, city, venue_name, event_date, description, capacity, registration_open U:8998; admin `POST /api/bridges/events` A:29612, publish A:29669) | `BuildingBridgesPortal.loadEventsFromAPI` P9:18619 | **EXISTS** | add `hero_image_url` column if a photo per event is wanted |
| Bridges | REGISTER (Boston card) → open registration form, no membership gate | anyone registers | anonymous: `POST /api/public-events/register {event:'building-bridges',…}` U:28900 (`PUBLIC_EVENT_REG` U:28873; QR email, Sheets); member: `POST /api/bridges/events/:id/register` U:16449 (returns QR), my-registration U:16506; server page `/building-bridges` U:1764 | `BuildingBridgesPortal.submitApplication` P9:19101 (falls back to `/api/bridges/apply` U:16515) | **EXISTS** | the website still points at a Google Form (WEBSITE audit §1D) — retarget to `/building-bridges` |
| Bridges | Past-city recap cards (Washington 01, London 02, New York 03, Zürich 04): guests, new connections — admin-editable | data | none — `pastEvents` hard-coded P9:18515–18560; `bridges_events` has no recap fields | `BuildingBridgesPortal` render P9:18941–18960 | **MISSING** | add `bridges_events.recap_guests`, `recap_connections`, `recap_summary`, `edition_no` (both mirror blocks) + admin edit via `PUT /api/bridges/events/:id` A:29628; past rows = `status='past'` |
| Bridges | Per-edition photo galleries (admin uploads) | gallery | none | — | **MISSING** | `bridges_photos` (event_id, url, caption, sort) + admin upload (multer/Cloudinary) + `GET /api/bridges/events/:id/photos` |
| Bridges | Program & speakers per event | show | `GET /api/bridges/program` U:16643, `GET /api/bridges/speakers` U:16570 (admin A:29862/A:29794, publish A:29902/A:29835) | `App.toggleBridgesProgramPublish` P9:30758 (staff) | **EXISTS** | — |
| Bridges | "No Harvard branding", targeting copy only | copy | — | — | **EXISTS** | content rule |

## H. Network — People (Network.dc.html)

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Network | Smart search: one field matching name, institution, specialty, city, program participation | server-side matching | `GET /api/networking/discover` U:26423 returns **all** public users (id, names, institution, country, bio, photo, career_stage, research_interests) — no search params, no city/specialty/program; `GET /api/plexus/attendees?search=&country=&institution=&interests=` U:22031 (registered attendees only); `GET /api/member/search` U:13470 (forum members only) | `NetworkingPortal.loadDiscover` P9:16952, `_normalizeMember` P9:16921, seed list P9:16904–16920 | **MISSING** — cross-field directory search | build `GET /api/network/search?q=&page=` over `users` ⋈ `networking_profiles` ⋈ `user_profiles` ⋈ project membership (`registrations`, `gala_registrations`, `forum_members`, `bridges_registrations`); add `users.city` + `users.specialties` (JSON) columns; delete the seed list |
| Network | "People for you": requests first, then suggestions with reason chips (shared field, attends Plexus, mutual contacts) computed server-side | suggestions | pending requests `GET /api/networking/connections/pending` U:26521; mutual counts `GET /api/networking/mutual-counts` U:14355, `GET /api/networking/mutuals/:userId` U:14376; coffee match `GET /api/networking/coffee-match` U:26696 | `loadPendingRequests` P9:16865 (`#requestsGrid` IX:9434), `loadCoffeeMatch` P9:17991, `ConnectionRecommendations` P9:53153 (client heuristics) | **PARTIAL** — reasons not computed server-side | build `GET /api/network/suggestions` → `[{user, reasons:['shared_specialty','attends_plexus','mutual:3']}]` |
| Network | My network (connections) | list | `GET /api/networking/connections` U:26499 | `NetworkActions` P9:833 | **EXISTS** | legacy `connections` table routes U:13802–13821 also exist — use the `networking_*` family |
| Network | BROWSE ALL 50 MEMBERS → paginated full directory | paginated | U:26423 has no pagination (client `pageSize:12` P9:16901) | directory view P9:16899–16903 | **PARTIAL** | add `?page=&limit=` to U:26423 (or the new search route) |
| Network | CONNECT / ACCEPT / DECLINE | server state | `POST /api/networking/connections` U:26473 (pushes notification), `PUT …/:id {accept|reject}` U:26509, pending U:26521 | `sendConnectionRequest` P9:17811, `acceptRequest` P9:17854, `NetworkActions.acceptConnection` P9:834 | **EXISTS** | — |
| Network | MESSAGE → thread | open DM | `POST /api/messages` U:26727 | `loadConversationsList` P9:17180 | **EXISTS** | — |
| Network | From the Forum teaser above People for you | same store as Home | `GET /api/forum/posts` U:17228 | — | **PARTIAL** (see F-4) | — |
| Network | Request an intro (existing) | keep | `POST/GET/PUT /api/intro-requests` U:14397/U:14422/U:14448 | `DirectoryPowerups.submitIntro` P9:47724 | **EXISTS** | — |

## I. Messages (Messages.dc.html) — in-portal inbox

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Messages | Thread list + conversation + composer; unread markers | member ↔ member | `GET /api/messages` U:26778 (latest per partner; **no unread count**), `GET /api/messages/:userId` U:26808 (paginated), `POST /api/messages` U:26727 (`direct_messages`, push to receiver) | `NetworkingPortal.loadConversationsList` P9:17180, `pollCurrentChat` P9:17183 | **PARTIAL** — unread counts | add `unread` per conversation (`is_read`/`read_at` U:9275, U:8529) and `PUT /api/messages/:id/read` |
| Messages | Official coordinators thread ("OFFICIAL · MED&X TEAM"); replies land in admin inbox | member ↔ team | `GET /api/user/admin-messages` U:28050, read U:28060, reply `POST …/:id/reply` U:28071; admin side `POST /api/admin/messages` A:34328, thread A:34407, list + AI triage A:34302 | `openAdminMessages` P9:17184, `replyToAdmin` P9:17185, `NotificationSystem.openAdminMessage` P9:53081 | **EXISTS** | tag the thread OFFICIAL when `sender_type='admin'` |
| Messages | Every MESSAGE US button opens the inbox with identity attached; topic tags (admin note 24) | new thread to the team with a topic | `POST /api/assistant/escalate {target, category}` U:12676, `POST /api/accelerator/ask-coordinator {subject}` U:14566, `POST /api/purchases/inquiry` U:12482 — all write `direct_messages` with `title` | `MedXAssistant` P13:77, `submitPurchaseInquiry` P9:2378 | **PARTIAL** — no generic route, no topic column | add `POST /api/messages/team {topic, body}` and `direct_messages.topic TEXT` (mirror block); zero `mailto:` in the new UI |
| Messages | Unread badge on INBOX tab / REQUESTS badge | live counts | admin messages `is_read`; pending connections U:26521 | — | **PARTIAL** | add `GET /api/me/counters` → `{inbox_unread, requests, notifications_unread}` |

## J. Profile & settings (Profile.dc.html)

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Profile | SAVE CHANGES → PATCH profile; every field persists | server write | `PUT /api/auth/profile` U:11925 accepts `first_name, last_name, phone, institution, country, bio, is_public_profile` (`users` U:6160); `PUT /api/networking/profile` U:26446 (`research_interests`, `career_stage`, `looking_for`, `open_to_coffee_chats`); `GET /api/auth/me` U:11906 | `SettingsPortal.saveProfile` P9:23882–23930 (**localStorage only**, `// TODO` P9:23906), form ids `#settingsFirstName…#settingsLinkedIn` | **PARTIAL** — UI not wired; `title`, `specialty`, `linkedin` have no `users` columns | wire to U:11925; add `users.title`, `users.linkedin_url`, `users.city`, `users.specialties` (JSON) + mirror-block ALTERs; extend U:11925 |
| Profile | UPLOAD PHOTO | store + serve | `users.photo_url` column exists (U:6163); generic upload `POST /api/upload/:type` U:19678 (member JWT, Cloudinary in prod); **no route sets `photo_url`** | `SettingsPortal.uploadPhoto` P9:23933–23947 (preview only) | **MISSING** | build `POST /api/me/photo` (multer + `cloudUpload('avatars')` U:5570 → `UPDATE users SET photo_url`) and `DELETE /api/me/photo` |
| Profile | Specialty chips: fixed suggestions + free-text custom tags (stored as strings) | persist | none for members (`forum_members.specialty` U:6582 is forum-only) | chips toggle (mock) | **MISSING** | `users.specialties TEXT` (JSON array) via U:11927 |
| Profile | Country dropdown (full list) | list | — | `populateCountryDropdowns()` P9:3152 | **EXISTS** | — |
| Profile | Completion meter (same source as Home) | server % | see B-3 | `ProfileCompleteness` P9:48739 (localStorage) | **MISSING** (B-3) | — |
| Profile | Directory / updates toggles | persist | `is_public_profile` (U:11925), `open_to_coffee_chats` (U:26446), follows `notify_topics` U:14084 | `NetworkingPortal.saveProfile` P9:18402 | **EXISTS** | — |
| Profile | Directory preview card | render self | `GET /api/auth/me` U:11906 + `GET /api/networking/profile` U:26464 | `ProfileCard` P9:50131 | **EXISTS** | — |
| Profile | EN·HR segmented control | persist | `PATCH /api/me {locale}` U:13708 | `MedXI18n.set` P1 (toggle `.up-lang-toggle` IX:181) | **EXISTS** | — |
| Profile | Account: change password, download my data, delete account | keep | `POST /api/auth/change-password` U:11885, `GET /api/auth/my-data` U:11214, `DELETE /api/auth/account` U:11144 | `SettingsPortal.changePassword` P9:23966, `downloadData` P9:24206, `deleteAccount` P9:24091 | **EXISTS** | — |
| Profile | Newsletter preferences (topics) | manage | see B-9 | — | **MISSING** (B-9) | — |
| Profile | Push notifications toggle | subscribe/unsubscribe | `POST /api/push/subscribe` U:23021, `DELETE /api/push/unsubscribe` U:23039, `GET /api/push/vapid-key` U:23016 | `MedXPush._persist` P9:52389, `unsubscribe` P9:52431 | **EXISTS** | — |

## K. My Med&X — wallet, tickets, record, rewards (My MedX.dc.html)

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Wallet | Member QR card; flips on tap to a QR back ("Jedna karta, sva vrata."); one QR opens all doors (identity + all current registrations) | member QR that scanners accept for any registered event | member badge token `GET /api/member/verify-link` U:12989 → `/verify/:token` U:2720; scanner side: `POST /api/admin/checkin/verify` U:27219 accepts regId UUID or **email** (U:27227–27229); cross-registration lookup `GET /api/admin/checkin/enrich` U:27466, admin `GET /api/admin/scan-context` A:11380, universal `POST /api/checkin` A:33704 | `MemberLinkCard` P9:29, `MedxCard` P9:2649, `loadPassport` flip P9:1990 (`_paintPassport`) | **PARTIAL** — the member QR resolves identity, but the check-in family keys on per-registration QR payloads (`{type:'MEDX_MEMBER', regId, evt}` U:529, U:4097–4133) | make the scanner resolve a `MEDX_MEMBER {userId}` payload to the person's registrations (reuse the email fallback U:27227 + enrich U:27466) and admit for the scanned event; add "Jedna karta, sva vrata" text to the flip |
| Wallet | Current tickets vs Past purchases tabs | split | `GET /api/my/events` U:13058 → `{upcoming[], past[]}` (registrations, gala, forum, bridges; `ticket` = `/qr/<id>.png`, `calendar`); gala detail `GET /api/gala/my` U:27760 (`qr_url`, `payment_status`, `invoice_number`) | `MyMedXPortal.renderMyEvents` P9:906 (`#mymedxMyEvents` IX:11666 — upcoming only, past = count line) | **PARTIAL** — past tab not rendered | render `past[]`; no new backend |
| Wallet | Per-ticket DOWNLOAD (PDF) | PDF ticket | hosted PNG only `/qr/:id.png` U:4077; ticket email builder `buildTicketQrBlock` U:20826 | `PassSystem.downloadPass` P9:458 (html2canvas PNG) | **MISSING** | build `GET /api/my/events/:kind/:id/ticket.pdf` (pdfkit: event facts + QR PNG buffer via `QRCode.toBuffer`) |
| Wallet | Per-ticket EMAIL TO ME | resend confirmation | admin-only `POST /api/admin/registrant/:type/:id/resend-ticket` A:35517 | — | **MISSING** | build member `POST /api/my/events/:kind/:id/resend` (owner check, reuse the admin resend body) |
| Wallet | Per-ticket ADD TO WALLET (Apple / Google) | passes | Google ticket `GET /api/member/wallet/google/ticket/:regId` U:13386 (gated on `GOOGLE_WALLET_SA_KEY` U:13388–13392), membership pass U:13296; Apple `GET /api/member/wallet/apple/ticket/:regId` U:13441 = stub (`apple_pkpass_not_implemented` WL:272) | `addTicketToGoogleWallet` P9:2180, `addToGoogleWallet` P9:2041, `addTicketToAppleWallet` P9:2203 (flag `_APPLE_TICKET_WALLET_ENABLED` P9:2202) | **PARTIAL** — Google ready when the SA key is set; Apple needs certs + pkpass builder | Apple: implement `providers.apple.buildPass` in `shared/wallet.js` with `passkit-generator` once `APPLE_WALLET_*` exist |
| Wallet | RECEIPT (paid items) / CONFIRMATION (free items) | downloadable | invoice JSON `GET /api/plexus/registration/:id/invoice` U:20060; gala `invoice_number` U:27760; admin HTML "pdf" `GET /api/finance/invoices/:id/pdf` U:25137 (admin only) | — | **MISSING** | build `GET /api/my/receipts/:kind/:id.pdf` from `invoices` + `payment_transactions`; confirmation = the ticket PDF |
| Wallet | DOWNLOAD CARD (member-card PDF) | PDF | none (client PNG via `MedxCard` P9:2649) | `MedxCard` | **PARTIAL** | server PDF optional; client PNG acceptable if the spec allows |
| Wallet | ADD TO PHONE WALLET (member card) | Google/Apple | `GET /api/member/wallet/google` U:13296 | `addToGoogleWallet` P9:2041 | **PARTIAL** (Apple stub) | — |
| Wallet | OPEN REWARDS → rewards / points ledger | page | `GET /api/rewards/summary` U:26360 (balance, ledger, tiers, coupons), `POST /api/rewards/redeem` U:26381, claim-missing U:26313; `/api/rewards/sync|earn|purchase-checkout` are 410 (U:26374–26376) | `RewardsPortal` P9:519 (`load` P9:544, `confirmRedeem` P9:700) | **EXISTS** | quiet members are redirected away from rewards (P9:4415) |
| Wallet | PROJECTS I FOLLOW chips (add/remove) | follow endpoints | `GET/POST /api/notify-topics` U:14079/U:14084 | `MedXNotify` P7:6 | **EXISTS** | — |
| Wallet | MY INTERESTS chips | persist | `PUT /api/networking/profile {research_interests}` U:26446 (comma string) | `NetworkingPortal.saveProfile` P9:18402; `mymedx_interests` localStorage | **PARTIAL** | write to `networking_profiles.research_interests`; drop the localStorage key |
| Wallet | CHANGE / SWITCH rows → settings modals | reuse | see J | `SettingsPortal` P9:23794 | **EXISTS** | — |
| Wallet | Record: events attended, certificates, passport stamps, wrapped | show | `GET /api/member/record` U:13109, `GET /api/member/passport` U:13159, `GET /api/member/wrapped` U:13203, `GET /api/plexus/my-certificate` U:22301, `GET /api/plexus/cme/my` U:21491, giving U:13674 | `renderRecord` P9:1107, `loadPassport` P9:1990, `openWrapped` P9:2486, `renderMyCme` P9:946, `renderGiving` P9:983 | **EXISTS** | — |
| Wallet | Share record / verify link | links | `GET /api/member/share-record-link` U:13011 → `/r/:token` U:2909; `GET /api/member/verify-link` U:12989 | `shareRecord` P9:1316, `_fetchVerifyLink` P9:1344 | **EXISTS** | — |
| Wallet | Guest passes ("bring a colleague") | keep | `GET/POST /api/guest-passes` U:12835/U:12854, revoke U:12924 | `GuestPass` P12:2 (`renderEventCard` P12:69, `revoke` P12:155) | **EXISTS** | — |
| Wallet | Purchase inquiry / refund question | keep | `POST /api/purchases/inquiry` U:12482 | `submitPurchaseInquiry` P9:2378 | **EXISTS** | — |

## L. Auth flow (Auth.dc.html)

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Auth | Sign in | JWT | `POST /api/auth/login` U:11098 (`authLimiter`; failed attempts audited) | `UserPortal.login` P9:3892 (`#userLoginBtn` IX:1033), lockout `sessionStorage.medx_login_attempts` | **EXISTS** | website modal uses the same route |
| Auth | Create account — 3 steps: details → confirm email → in; explore before confirming, banner nags | register returns a JWT and sends the verification link | `POST /api/auth/register` U:11037 (`email, password, first_name, last_name, institution, country, locale`; issues JWT + verification email U:11334; welcome drip); confirm `GET /api/auth/verify?token=` U:11704 → `/?verified=true|already|expired|invalid` | `UserPortal.register` P9:4130 (`#userRegisterBtn` IX:1151, consent `#registerConsent` IX:1146), step-3 confirm UI + `?verified=` handling P9:3403–3434 | **EXISTS** (email delivery blocked until Brevo key) | — |
| Auth | Reset password | email link → form | `POST /api/auth/forgot-password` U:11753, page `GET /reset-password/:token` U:11792, `POST /api/auth/reset-password` U:11858 (invalidates old JWTs via `password_changed_at` U:6069–6076) | `UserPortal.openForgotPassword` P9:3815 | **EXISTS** | the reset page is server-rendered (`premiumPage`) — restyle server-side |
| Auth | Forum invitation-code entry (with or without an account) | redeem + create inline | see F-1 | `ForumPortal.verifyInviteCode` P9:15007 | **MISSING** (F-1) | — |
| Auth | Welcome landing / split photo layout / no social logins | static | — | splash markup IX:181–191 (`.splash-lang-toggle`) | **EXISTS** | — |
| Auth | Website hand-off (`?mxt=`) and Stripe returns land signed in | keep | IX:54; boot P9:3362–3512 | — | **EXISTS** | keep first in `<body>` |
| Auth | Session expiry / password-change invalidation | keep | `auth` U:6088–6119 (rejects tokens issued before `password_changed_at`) | `UserPortal.api` P9:4693 (401 handling) | **EXISTS** | — |

## M. Newsletter + Emails (Emails.dc.html)

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Emails | Confirm-email template (600 px, ink header, crimson/gold rule, cream body, Fraunces) | restyled transactional shell | `buildEmailTemplate(title, bodyHtml, locale)` U:425 (used by every member email); verification email U:11300–11310 (resend) and signup link U:11338; logo `EMAIL_LOGO_URL` U:302 | — (server-side HTML) | **EXISTS** (restyle) | admin twin `buildEmailTemplate` A:257–259 (footer links) must match |
| Emails | Ticket confirmation with embedded QR | QR card | Plexus welcome U:20001; Stripe fulfilment email U:20819–20832 (`buildTicketQrBlock` U:20826, hosted `/qr/<id>.png`); gala approval U:26971; bridges/donor `PUBLIC_EVENT_REG` emails U:28873; sign-up form ticket U:29303 | — | **EXISTS** (restyle) | keep hosted-PNG QR (Gmail strips `data:` U:498) |
| Emails | Monthly newsletter (topics; footer Manage topics + Unsubscribe) | send + preference center | admin `POST /api/admin/newsletters/:id/send` A:23945 (`memberNewsletterHtml` A:23959, digest links A:23092–23106), segments A:23064, interests A:23576; member preference center `/email-prefs?e=&s=` U:1830 (scopes `reminders,newsletter` only, `email_optouts` U:9390), one-click `/unsubscribe` U:1860, link builder `emailPrefLink` U:1784 | — | **PARTIAL** — per-topic preferences missing | extend `email_optouts.scopes` to topic keys and render topic checkboxes on U:1830; "Manage topics" link = `emailPrefLink` |
| Emails | Attendance card auto-generated + emailed on registration/payment ("I'm attending Plexus 2026") | server job | client generator `ImAttendingCard` P9:2850 (html2canvas, consent `POST /api/member/card-consent` U:13278); admin CardStudio `GET /api/admin/cards/roster` A:38292 + `POST /api/admin/cards/send` A:38314 (stages rendered cards into the outbox) | `ImAttendingCard.download` P9:3028 | **MISSING** — server-side render + auto-send | build a renderer (headless Chrome exists on the admin: `psRenderPdf` A:37435; or SVG→PNG with `sharp`) triggered from the Stripe webhook / registration insert, staged into `scheduled_emails` |
| Emails | Year-in-review card emailed at year end | job | data `GET /api/member/wrapped` U:13203; renderer `openWrapped` P9:2486 (client) | — | **MISSING** | yearly job on the admin drainer (`cron` next to A:43350) rendering per member |
| Emails | Newsletter signup on the public site FAQ should point at the portal | website copy | — | — | **MISSING** (website content, not portal) | edit `faq.html` in the Netlify digest (WEBSITE audit §5.4 rules) |

## N. Notifications & push

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Notifications | Bell list, mark read, mark all, click-through targets | keep | U:23057, U:23081, U:23088; targets `link` column; announcements U:23218 | `NotificationSystem` P9:52647 (`location.hash = notif.link` P9:52918) | **EXISTS** | — |
| Notifications | Web push enable + SW handlers | keep | `GET /api/push/vapid-key` U:23016, subscribe U:23021, unsubscribe U:23039; drain U:205–229; SW push SW:121–143, click SW:144–161, resubscribe SW:162–176 | `MedXPush` P9:52329 | **EXISTS** | admin composer A:22948 with `push` flag |
| Notifications | Admin announcements with link + show-until (admin note 24) | keep | `user_notifications.expires_at` (U:23063 filter), `member_announcements.link_section` U:9578 | as above | **EXISTS** | — |

## O. Certificates · mentorship · opportunity board · assistant

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Certificates | Attendance certificate after check-in; public verify page | issue + verify | `GET /api/plexus/my-certificate` U:22301 (creates `certificates` row when checked in), `GET /verify-certificate?n=` U:22265, org signature U:12067; admin issuing A:2689/A:19815 | `MyMedXPortal.downloadCertificate` P9:1245, `PostEvent.downloadCertificate` P9:45346 | **EXISTS** | server PDF optional (pdfkit) |
| Mentorship | existing page | keep | see A-8 | see A-8 | **EXISTS** | — |
| Opportunity board | existing page | keep | see A-9 | see A-9 | **EXISTS** | — |
| Assistant | FAQ assistant widget (kept, optional) | keep | `POST /api/assistant/ask` U:12581, feedback U:12662, escalate U:12676 (`faq-kb.js`; AI enhancer `faqRephrase` U:12564 mock without key) | `MedXAssistant` P13:87–137 | **EXISTS** | — |

## P. Empty states · system pages · PWA / mobile · app icon

| Screen | Control / behaviour | What it must do | Existing endpoint(s) / table | Existing frontend logic to port | Status | Notes |
|---|---|---|---|---|---|---|
| Empty states | First-day states for wallet, inbox, news, network (italic line + why + CTA) | pattern | — | `EmptyState.render` P9:51668, `MedXState.render` P9:3211, feed fallback `errorFallbackHtml` P9:47120 | **EXISTS** (restyle) | — |
| System pages | 404 in brand voice | render for unknown routes | SPA fallback serves `index.html` for extensionless paths U:29428–29434 (404 plain text for paths with an extension U:29428–29430); `/api/*` JSON 404 U:29418–29420; server notice pages use `PLEXUS_SHELL`/`premiumPage` (e.g. U:1293) | — | **PARTIAL** — client must render a 404 state for unknown hashes/paths | add an unknown-section branch in `showSection` P9:4413 and a branded server 404 for extension paths |
| System pages | Maintenance page | downtime page | none | — | **MISSING** | add a `MAINTENANCE=1` env + middleware before `express.static` U:5462 returning the branded page with 503 (keep `/health` 200 so Render does not roll back) |
| Mobile | 430 px app pattern: bottom tab bar HOME / PROJECTS / PEOPLE / INBOX / MY M&X, sub-views with back, 44 px targets | responsive shell | — | `MedXBottomNav` IX:1290–1300 (`#upBottomNav` IX:1269, tabs home/network/mymedx/profile/programs), `MobileOptimizations` P9:44052, `TouchGestures` P9:42707, `BottomSheet` P9:42643 | **PARTIAL** — tab set differs (no INBOX tab / unread badge) | rename tabs; INBOX badge from I-4 counters |
| PWA | "Add to home screen" flow kept; installable; offline shell | keep | `manifest.json` (`start_url /?app=1`, shortcuts), SW precache SW:6–14, network-first navigations SW:75–89, `/api/` never cached SW:68 | `PWA` P9:42347 (`beforeinstallprompt` P9:42369, `pwa_install_dismissed` P9:42374), `MedXInstall` P10:23 (iOS sheet), standalone detection P11 | **EXISTS** | bump `CACHE_NAME` (SW:2) with the redesign |
| App icon & splash | Fraunces ampersand icon (1024 master), splash screens | assets | `icon-192.png`, `icon-512.png`, `manifest.json` icons/`theme_color` | — | **PARTIAL** — new icon assets to be produced | keep file names so installed PWAs update in place |
| Offline ticket | boarding pass renders offline | keep | precached `/vendor/qrcode/qrcode.min.js` (SW:13) + `offline_<key>` cache | `PassSystem` P9:141 | **EXISTS** | — |

## Q. Product rules — cross-cutting (README "Product Rules" + notes 1–22)

| Rule | What it requires | Existing | Status | Notes |
|---|---|---|---|---|
| Admin-drives content: next-event panel, news, key dates, project cards, program rows, speakers, host institutions, hero photos, performers | one write path per list | `project_status` (A:11770), `feed_items` (A:12397), `plexus_settings.key_dates` (A:34551) + `accelerator_key_dates` (A:13535), `sessions` (A:20183/A:20250), `speakers` (A:20290/A:20844), `accelerator_institutions`/`accelerator_sites` (A:13657/A:12051), `portal_content.featured` (A:33888), `gala_settings` (A:30009) | **PARTIAL** — performers flag (D-4), cross-project key dates (B-7), next-event aggregate (B-2) | the admin "Member Pages manager" (admin note 22) should front exactly these tables |
| Contact = in-portal messaging, no `mailto:`, no "Ask the coordinators" form | one route | see I-3 | **PARTIAL** | 16 `mailto:` in IX today |
| Registration model: free conference; Gala only paid add-on; one external form; QR in My Plexus + wallet + phone wallets | flows | C-1, D-1, K-2, K-5 | **EXISTS** (Apple stub) | — |
| One QR opens all doors | scanner accepts the member QR | K-1 | **PARTIAL** | — |
| Attendance / year-in-review cards auto-emailed; no "Share your Med&X" section | jobs | M-4, M-5 | **MISSING** | remove `ImAttendingCard` UI entry once the job exists |
| Tickets: Download PDF / Email to me / Add to wallet; Current vs Past tabs; receipts | K-2…K-6 | — | **PARTIAL/MISSING** (see K) | — |
| Accelerator: embed the existing 7-step wizard; "Get notified" before opening; results by AX26 code | E | — | **EXISTS** | — |
| Dates consistent everywhere (Dec 4–5; Gala Dec 5 €150→€175 on Sep 1; Accelerator opens Dec 8 2026; 9th year; no abstracts) | data, not copy | `conferences`, `gala_settings.early_bird_deadline`, `accelerator_key_dates`; `MEDX_DATES` P9:7 hard-codes | **PARTIAL** | delete `MEDX_DATES`; read everything from B-2/B-7 |
| Profile completion: nudge disappears at 100 %, countdown expands | B-3 | — | **MISSING** (B-3) | — |
| State that must be server-driven (note 2): profile %, unread counts, request counts, wallet tickets, countdown targets, admin lists | endpoints | I-4, B-3, B-2 | **PARTIAL** | `GET /api/me/counters` + `GET /api/home/next-event` + profile-completion cover it |
| Buttons never wrap; placeholders; screenshot artefact warning; mobile out of scope in mocks | UI only | — | **EXISTS** | — |
| Newsletter is a real service (note 11) | B-9, M-3 | — | **MISSING** | — |
| Network search semantic-ish + reason chips (note 12) | H-1, H-2 | — | **MISSING/PARTIAL** | — |
| Forum = standing invitation-only network, max 200, annual (note 13) | F-1, F-2 | `forum_members` cap: none | **MISSING/PARTIAL** | enforce cap 200 in the redeem route |
| Bridges: no membership application; recap figures admin-editable (note 14) | G-2, G-3 | — | **EXISTS/MISSING** | — |
| Previous cohorts panel (note 15) | E-4 | — | **MISSING** | — |
| Talk Library removed (note 16) | A-11 | — | **EXISTS** (removal) | — |
| Easter eggs (note 17) | A-13, K-1 | — | **PARTIAL** | client-only |
| Auth 3-step, explore before confirming, forum code inline account (note 18) | L | — | **EXISTS** except forum code | — |
| Mobile pattern + PWA (note 19) | P | — | **PARTIAL** | — |
| Emails 600 px + newsletter footer (note 20) | M | — | **EXISTS/PARTIAL** | — |
| System pages + empty states voice (note 21) | P | — | **PARTIAL** | — |
| Forum feed is a real publish flow (note 22) | F-4 | — | **PARTIAL** | — |

---

## R. ADMIN redesign — destination → existing route groups → spec-only items

Route groups are the `SECTION_ROUTE_MAP` prefixes (A:1183–1239) plus the unmapped families every signed-in admin can reach (A:1176–1182). Permission ids are the 19 `PERMISSION_SECTIONS` (A:1153–1173). "Cross-origin" = the admin SPA calls the member backend.

| Destination (mock) | Existing route groups (admin server.js) | Permission section(s) | Still spec-only → MISSING |
|---|---|---|---|
| Today (Admin Home) | dashboard `GET /api/dashboard/summary` A:18035, `trends` A:18068, `portal-stats` A:18121; Action Center `/api/admin/nag/*` A:39128–39273; weekly pulse `POST /api/admin/pulse/run` A:23560; Executive Suite `/api/admin/advisors/*` A:42457–42664; per-admin prefs `/api/admin/sections` A:10946–10967, `/api/dashboard-preferences/:section` A:10981–11003; tasks `/api/admin/tasks*` A:12159–12364 + `/api/tasks*` A:18227–18339; pinned A:17929–17962; heartbeat A:35332; usage (founder) A:35359 | unmapped (+ `advisors`) | ✎ CUSTOMISE per-admin hero numbers (store in `admin_section_preferences`/`dashboard_preferences` — schema exists, UI spec-only); "Weekly Read headline = computed conclusions" (pulse text exists A:23560, headline extraction spec-only) |
| Plexus Hub (Before the week / Gala evening / After) | conferences A:11027–11139; registrations A:18868–18885, A:20059–20084, exports A:19030/A:19060; sessions A:20183–20267, rooms A:34649–34683; speakers A:20290–21026 (+ flights A:20497–20652, docs A:29914–29946, itineraries A:22123–22280, kits A:22719–22751); sponsors A:21116–21248, reports A:21879–21948; promo/coupons A:20165–20176, A:36424–36467; early-bird A:21472–21548; registration links A:36226–36335, combo links A:30971–31032; custom fields A:36350–36410; event components A:36197–36208; check-in A:32538–32848, A:33538–33845; seat confirmations A:38988–39033; waitlist A:12934–12963; testimonials A:21604–21721; post-event A:21766–21771, A:38922–38927; survey A:38941–38975; event invites A:40785–41215; reminders A:41526–41722; print suite A:38181–38218; auctions A:31294–31592; Q&A **cross-origin** to member U:21936–21982; CME A:34441–34508; page text A:30075–30079; Plexus settings A:34527–34551 | `plexus`, `conferences`, `cme`, `signup-forms`, `guest-passes` | **Program & schedule builder** as one screen (sessions/rooms routes exist; a member-page + PDF publish step = C-8); **Speakers manager** with itinerary per speaker (routes exist, unified screen spec-only) |
| Accelerator Hub | programs/years A:13455–13497; key dates A:13529–13589; institutions A:13598–13657 + sites A:12047–12086; applications A:13671–13794; criteria/scores A:13849–13908; interviewers A:15143–15495; rankings A:15743–15838; form-config A:16219–16256; files A:15858–16206; legacy review A:18748–18825; applicant portal A:24008–24363, `/apply` A:24908 | `accelerator` | Key dates → also feed member Home (B-7) |
| Accelerator Review Room | `/api/admin/review/*` A:14297–15109 (rubric, blind, reviewers, assignments, scorecards, decisions, letters, funnel); external reviewers `/api/review-access/:token*` A:14692–14746, page `/review` A:24377; interviewers A:15143–15495, `/evaluate` A:24551; research A:29000–29037 | `accelerator` | SEND INTERVIEW LINK = A:15420 (exists); ACCEPT → offer / DECLINE with undo = decisions A:14973 + letters A:15069 (undo = `revert` in A:14973); ranking CSV export (**MISSING** — only PDF A:15896) |
| Forum Hub | members/applications A:16726–16799; groups A:16799–16838; events A:16860–17020 (+ schedule A:17109–17145, resources A:17158, posts A:17172–17183, media A:17043–17093); check-in A:33485/A:33766; considerations A:39410–39441; candidates + campaign A:39591–40223; council invites A:38455–38581; convenings A:41736; gala-settings A:41761–41768; export A:18995; member-origin twins for invitations/templates/prospects U:18077–18265 | `forum` | **Forum Feed composer** (spotlight/news/note → `forum_posts`, F-4); **SEND CODE emails automatically** (U:18083 logs only); interactive world map (spec-only; counts from `forum_members.location_country` U:4358) |
| Bridges Hub | events A:29581–29681; registrations A:29688–29762; speakers A:29781–29835; program A:29847–29902; Croatians-abroad links/registrations A:30524–30568; check-in A:33518/A:33776 | `bridges` | **Post-event recap editor** (G-3 columns), **stats widget** ("Stats for media & sponsors": live counts exist via `/api/dashboard/*`; override + copy-line spec-only), follow-ups CRM rows (**MISSING** table `bridges_followups`) |
| Inbox (Outbox · Email registrants · Messages · Announcements · Newsletter · Team chat) | outbox A:38602–38716 (+ approve mirrors A:38695); bulk email A:34941–34976, registrant emails A:34935; messages A:34252–34407; user notifications A:22934–22982; member announcements A:11977–12036; newsletters A:23807–23995, segments A:23064, interests A:23576–23593, pr-newsletters A:23611–23650; team chat A:17236–17919 (channels, DMs, polls) | `member-ops`, `pr-media`, unmapped (chat) | APPROVE ALL per kind (batch approve exists per batch A:38646; grouping spec-only); hand-picked audiences (segments exist A:23064; per-person ticks **MISSING**); member topic tags (I-3 `topic` column); read/unread + archive on member threads (**MISSING** `archived` flag on `direct_messages`); announcement preview of the bell dropdown (client); reply-to + attachments in team chat (upload exists A:17702; reply-to column **MISSING**) |
| People | users A:11247 (profile, **calls member U:29193**), member meta A:12444–12449, member card toggles A:30689–30695, registrant activity/notes A:12540–12589, mark-paid/note/resend A:35495–35517, search A:35837, rewards A:35044–35161, guest passes A:22370–22469, member guest passes A:33212–33250, team A:35181–35398, contacts A:28575–29188 | `member-ops`, `guest-passes`, `team`, `contacts` | last-active (heartbeat exists for admins only A:35332; member `users.last_login` U:9397 exists) — full member analytics spec-only |
| Money | finance A:25546–26727 (dashboard, ledgers, invoices, payment/travel orders, reports); conference payments A:26904–27078; Stripe recent A:26875; reconcile A:35658–35805; transparency board pack A:22636–22655; sponsors ledger = `sponsors` A:21116–21171 (+ `auction_items` A:31294–31592) | `finances`, `plexus` | **Sponsors & donors ledger states pledge→invoiced→paid→thanked** (sponsor `status` exists A:21147; "thanked" + auto-landing of Donor Night pledges/auction results spec-only); **Bank import CSV/API** behind Reconcile (import exists A:35665 — API sync spec-only); "4 to chase" → gala unpaid rows exist (`payment_status`) |
| Calendar | year calendar A:12111–12147; timeline A:18552–18595; tasks (shared list) A:12159–12364 / A:18227–18339; sequences A:18348–18537 | `year-calendar`, unmapped | EXPORT PDF one-page year board (**MISSING**; print engine `psRenderPdf` A:37435 available); team tasks shared Today+Calendar = existing `project_tasks` (both read the same table — replace `localStorage medx_admin_tasks_v1`) |
| Event Day | gameday A:42909–43112 (settings, dashboard, messages, volunteer invites + tokenized volunteer login A:43053 → `/?gd=<token>` AIX:17751); staff tracking A:41875–42061; scanner A:32538–32848, A:33437–33704, resolve A:32576, roster for offline A:33652, undo A:19924; door list = `checkin/roster`; ops map = staff tracking live A:41941 | `gameday`, `plexus` | Rehearsal mode (amber, test data) **MISSING** (no `rehearsal` flag in `gameday_settings`); "activates automatically on event dates" (settings exist A:42929; auto-flip job spec-only); door-staff link = gameday volunteer invite (exists) |
| Settings | system health A:34694, test email A:34868; audit log A:35008; tech tools A:35962–36169; team access + permissions A:35181–35398 (`allowed_sections` JSON, founder-only A:35290/A:35303); files/resources A:37151–37194, A:18619–18719; org signature A:38426–38441; content blocks (website text) A:11927–11940; portal-config A:33533; design presets A:38059–38129 | `tech`, `team`, `files`, `pr-media` | **Website-text editor for medx.hr** beyond the 7 whitelisted `content_blocks` keys (A:11940 whitelist) — spec-only; Roles = Admin + Scanner staff (`is_admin`/`is_staff` exist A:35375) |
| Studio | print suite A:38181–38218 (badges, banner, backdrop; headless Chrome A:37435); design assist A:38129; content studio A:37077–37367 (assets, video); planner A:36885–36962; PR media A:27711–27772; sponsor reports PDF A:21930; certificates via post-event round A:38927; sign-up form pages A:30710–30902; 3D planner = external `plexus-tables.netlify.app/planner.html` (AIX:10596, AIX:13321) | `plexus`, `pr-media`, `signup-forms` | Social cards from live guest lists = attendance cards A:38292/A:38314 (exists); brand-kit downloads (static); **3D ballroom walk-through** spec-only (2D seating A:12605–12755 is the base) |
| Member Pages ("What members see") | project status A:11750–11770; feed items A:12393–12431; portal content A:33876–33933; plexus page text A:30075–30079; gala settings A:29967–30009; accelerator overview A:13497–13511; content blocks A:11927–11940; custom registration fields A:36350–36410 | `pr-media`, `plexus`, `accelerator` | **Form builder** (custom questions per event form: `event_custom_fields` A:36364 exists for registration links; per-event editor UI spec-only); "Saved ✓" instant persistence = existing PUT routes; registration-form editor beyond custom fields spec-only |
| Gala (new screen) | registrations A:30094–30189 (approve/pay-link), menu options A:30212–30248, seating A:12605–12755 + picker sync A:12872–12904, who-is-coming A:30264, guest message draft/queue A:30310–30343, program notify A:30379, invite links A:30452–30499, scan/check-in A:33437/A:33465, refund **cross-origin** U:736, waitlist A:12934–12963 + offers A:39033–39062 | `plexus` | Kitchen counts CSV (menu options exist; export spec-only); **waitlist auto-open on sell-out + 24 h auto-offer** (offers + `release-unconfirmed` A:39007 exist for the conference; gala-specific automation spec-only); add guest (invoice/VIP/sponsor) = VIP invite links A:30452 + comp registration (spec-only for gala); non-refundable cancel with undo (**MISSING** soft-cancel state) |
| Registrations (new screen) | all-events list: `/api/admin/registrations` A:18868, plexus A:20084, gala A:30094, forum A:16992, bridges A:29688, Croatians-abroad A:30555, sign-up forms A:30826; bulk email A:34976; CSV exports A:19030/A:18995/A:30902; per-registrant file A:12540–12589, A:35495–35517 | `plexus`, `forum`, `bridges`, `signup-forms` | **One cross-event table with source-link tag** (each table has its own list; a union endpoint like member `GET /api/my/events` U:13058 but admin-wide is **MISSING**; `registration_links.token` on rows exists for plexus/invite only) |
| Links (new screen) | registration links A:36226–36335 (create/list/deactivate, `.url` per row), gala invite links A:30452–30499, Croatians-abroad links A:30524–30547, combo links A:30971–31032, sign-up form QR A:30888; member side counts uses U:1302, U:20752 | `plexus`, `bridges` | visit counts (**MISSING** — only `uses`), pause/resume (`is_active` deactivate exists A:36335; resume **MISSING**), typed links PUBLIC/VIP/DIASPORA/SPONSOR with use limits (`max_uses` exists; type taxonomy = `label`/`link_type` today) |

**Spec-only list from the README (no mock, build from spec):** 1 Program & schedule builder → Plexus Hub (routes exist; screen + member-page publish + PDF C-8). 2 Speakers manager → Plexus Hub (routes exist). 3 Form builder → Member Pages (`event_custom_fields` exists per link; per-event editor MISSING). 4 Website-text editor → Settings (MISSING beyond 7 keys). 5 Admin DMs → Inbox (`POST /api/teamchat/dm` A:17791 exists; v2 UI). 6 Auctions tool → exists (A:31294–31592), one link deep. 7 3D ballroom walk-through → MISSING (external planner). 8 Merch studio (external Netlify, AIX:7875) and rewards ledger (A:35044–35161) → exist one link deep. 9 Bank import → CSV exists (A:35665), API sync MISSING. 10 Member analytics → MISSING (page-view beacon `page_views` U:12319 + admin analytics A:18915 are site-level only).

---

## S. Summary count — member redesign rows by screen

| Screen / section | EXISTS | PARTIAL | MISSING | Rows |
|---|---|---|---|---|
| A. Global chrome | 12 | 1 | 0 | 13 |
| B. Home | 4 | 5 | 2 | 11 |
| C. Plexus (overview · program · Zagreb · My Plexus) | 12 | 2 | 1 | 15 |
| D. Gala | 4 | 2 | 0 | 6 |
| E. Accelerator + wizard | 11 | 2 | 1 | 14 |
| F. Forum | 4 | 2 | 2 | 8 |
| G. Building Bridges | 4 | 0 | 2 | 6 |
| H. Network | 3 | 3 | 1 | 7 |
| I. Messages | 1 | 3 | 0 | 4 |
| J. Profile | 6 | 1 | 4 | 11 |
| K. My Med&X wallet | 6 | 6 | 3 | 15 |
| L. Auth | 5 | 0 | 1 | 6 |
| M. Newsletter + emails | 2 | 1 | 3 | 6 |
| N. Notifications & push | 3 | 0 | 0 | 3 |
| O. Certificates · mentorship · opportunities · assistant | 4 | 0 | 0 | 4 |
| P. Empty states · system · PWA · icon | 3 | 3 | 1 | 7 |
| **Member total (A–P)** | **84** | **31** | **21** | **136** |
| Q. Product rules (cross-cutting, rolled up; last status of combined cells) | 5 | 12 | 6 | 23 |
| R. Admin destinations (17 rows) | routes exist for every destination | 11 destinations carry PARTIAL items | 10 spec-only items | 17 |

**The five backend pieces the member redesign cannot ship without:** (1) `POST /api/forum/redeem-code` + real SEND CODE emails (F-1); (2) `GET /api/me/profile-completion` (B-3, J-5) and the Profile write path (`POST /api/me/photo`, new `users` columns, wire `PUT /api/auth/profile`) (J-1, J-2, J-3); (3) member newsletter endpoint + topic opt-outs (B-9, M-3); (4) `GET /api/network/search` + `GET /api/network/suggestions` with new `users.city/specialties` columns (H-1, H-2); (5) wallet ticket services — PDF ticket, resend-to-me, receipts (K-3, K-4, K-6) — plus the attendance-card job (M-4). Everything else on the list is a restyle over existing routes, provided `BREVO_API_KEY` and `CLOUDINARY_URL` are set on Render (§4/§5 of the master document).

*Generated 2026-08-28 from HEAD `8b7ba23`; companion: `design/PORTAL-CONNECTIONS-MASTER-2026-08-28.md`.*

# UXFIX-M2 · 2026-09-02 — member-portal STYLE/media fixes (audit items 3 · 4 · 7 · 10 · 13)

Agent M2. Scope owned: `user-portal/frontend-v2/js/views/accelerator.js`, `css/**`, `assets/**`,
plus the registration-form reskin. Source: `deploy/staging/UX-AUDIT-MEMBER-2026-09-02.md`.
No git, no deploys, no emails. Everything below the "Instructions for other owners" line is
**not** applied — those files belong to other agents.

---

## Done in M2-owned files

### #3 — registration form reskin → patch file (form lives in shared `server.js`)
The audit's navy generic-SaaS screen is the server-rendered `/plexus` page inside
`user-portal/backend/server.js` (`PLEXUS_SHELL`, lines ≈1159–1291 + route ≈1295–1615) — a 29k-line
shared backend file, not a standalone view, so per the ownership rule the full prescribed reskin
is delivered as **`deploy/staging/UXFIX-M2-register-reskin.patch.md`** (11 exact old→new blocks:
house-token shell with Fraunces/Inter Google-Fonts head, cream/ink/crimson/gold, hairline inputs
with crimson focus, square corners, gold-dark FREE labels replacing the green chips, ink total
band, crimson submit, `galaKeynoteBlock(light)` parameterization so the two dark invite surfaces
stay pixel-identical, plus all dark-theme inline colours in the client JS). Styling reference:
`git show main:user-portal/backend/boston.js` (FONTS_HTML + micro-label/hairline-input
vocabulary), applied with the portal's exact tokens from `frontend-v2/css/tokens.css`.

**Already fixed via assets (mine):** the audit's "empty grey speaker-photo boxes" on the form were
`/assets/gala/kn512_*.jpg` + `mus512_*.jpg` 404-ing on the Netlify origin (the form HTML is
proxied from Render; the browser resolves `/assets/…` against the portal origin, where
frontend-v2 lacked the files). Copied all six from `user-portal/frontend/assets/gala/` into
`user-portal/frontend-v2/assets/gala/` (36–58 KB each) — the four keynote portraits + the two
musicians now render on the form even before the reskin patch is applied.

### #4 — points-and-coupons economy: nothing lives in M2 files (verified)
Swept `css/app.css`, `css/tokens.css`, `css/views/*.css` and `js/views/accelerator.js` for
`points|pts|reward|coupon` — zero style hooks or markup; the entire economy is inline in
`me.js`/`chrome.js`/`plexus.js` (owned elsewhere — exact removals below). No CSS deletions needed.

### #7 — striped placeholder art replaced (Accelerator cohort blocks)
`js/views/accelerator.js`:
- `COPY.cohorts.photo1/photo2` label strings → `{src, alt}` objects pointing at two new real
  photos; the two stripe `<div>`s in `blockTeam()` ("PREVIOUS COHORTS" grid) now render
  `<img … object-fit:cover>` with the stripe kept underneath as the degrade state, and a
  gala.js-style `error → remove()` binding added in `render()` (no inline handlers).
- New assets (recompressed from the medx.hr live-site mirror
  `MedX_Squarespace/site_live_mirror_2026-07-31/assets/photos/`):
  - `assets/ax-cohort-arrival.jpg` — 240 KB, 1100px, from `acc_25_2.jpeg` (fellow at the
    Massachusetts General Hospital sign, Boston — literal "cohort arrival, Boston").
  - `assets/ax-lab-day.jpg` — 276 KB, 1200px, offset-cropped from `acc_25.jpeg` (two fellows at
    the bench — literal "lab day"); faces verified in-frame after crop.
  Both photos were already published on the live medx.hr site (same mirror).

### #10 — gala speaker grid (CSS-solvable parts)
- `css/views/gala.css`: with exactly five speakers the 4-col stage grid stranded one card —
  added `[data-screen-label="Gala Evening"] .mx-grid-4:not(.mx-gala-gallery):has(> div:nth-child(5):last-child)
  { grid-template-columns:repeat(5,1fr) }` at ≥901px → one even row of five; harmless 4+1
  fallback without `:has()`. The uniform treatment (square crop, `object-fit:cover`) already
  lives inline on every card — verified identical between gala and plexus (`.mx-portrait`'s
  `object-position:top` ≡ gala's `center top`).
- **Anderson's file replaced** (assets are mine): `assets/gala/gala_keynote_anderson.jpg` was a
  pre-cropped circle-badge headshot floating on white (400×400, photo only in the inner circle →
  pixelated). Rebuilt from the mirror's rectangular `kn_anderson.jpg` as a proper full-bleed
  512×512 square crop (50 KB), same path/filename so the admin-entered speaker image path keeps
  working.

### #13 — 44px phone tap targets (CSS)
`css/app.css`, new `@media (max-width:480px)` block:
- Section-tab strips → one left-aligned horizontally scrollable row (no wrap, hidden scrollbar);
  every tab link/span becomes `inline-flex, min-height:44px`. Type size untouched.
- Breadcrumb links/spans → `min-height:44px` the same way (rows keep wrapping).
- `.chip` → `min-height:44px, inline-flex` (speaker filters, topics, specialties).
- Hooks: `.mx-plexus-tabs` (existing) + `.mx-tabs`/`.mx-crumbs` (added to accelerator.js's own
  strip/crumbs); strips and crumb rows in views without hooks are matched by their verbatim
  artboard inline signatures (`.mx-gutter[style*="gap:26px;padding:13px 36px;border-bottom"]`
  for tabs — plexus/messages/accelerator; `[style*="gap:13px;padding:10px 36px"]` for crumbs —
  gala/bridges/forum/network/messages/profile/plexus) — the same `[style*=…]` technique the
  export's own responsive.css used. `mx-stats` (gap:24px) and me.js's `padding:10px 36px 34px`
  wrapper verifiably do NOT match. The mobile bottom bar `#mx-tabbar` already carries
  `min-height:44px` inline — untouched.

---

## Verification (all pass)

1. `node --check` on `js/views/accelerator.js` (parsed as ESM) — OK.
2. All 11 `css/**` files run through a parser (comment/string stripping, brace/paren balance,
   declaration-shape check per block) — all OK.
3. Every added/replaced image ≤300 KB and every `/assets/…` path referenced by accelerator.js
   exists on disk: ax-cohort-arrival 240 KB · ax-lab-day 276 KB · gala_keynote_anderson 50 KB ·
   kn512 ×4 (42–58 KB) · mus512 ×2 (36–53 KB) — 9/9 OK, refs 3/3 exist.

---

## Instructions for other owners (NOT applied — exact edits)

### A · me.js owner — #4 delete the rewards economy (`js/views/me.js`)
1. **COPY block** (lines ≈29–41): delete the whole `rewards: { … }` entry from `COPY`.
2. **load()**: line ≈130 delete `rewards: api.get('/api/rewards/summary'),`; line ≈164 delete
   `rewards: r.rewards, ` from the returned object.
3. **Band**: delete `function blockRewardsBand() { … }` (lines ≈289–302) and its single call
   `${blockRewardsBand()}` at line ≈613 in the wallet template.
4. **Tab**: delete `function rewardsTab() { … }` (lines ≈531–605) and the template branch at
   ≈608–609 (`if (st.view === 'rewards') return …rewardsTab()…`).
5. **Route**: line ≈913 change
   `const view = tab === 'certificates' ? 'certificates' : tab === 'rewards' ? 'rewards' : 'wallet';`
   → `const view = tab === 'certificates' ? 'certificates' : 'wallet';`
   (bookmarked `/app/me/rewards` then lands on the wallet — no dead route).
6. **Handlers**: delete `redeem:` (≈873–889), `copyCode:` (≈890) and `moreLedger:` (≈891);
   `ledgerN: 12` in st (≈919) becomes unused — delete too.
7. Update the header comment (lines 3 and 6) that names the "REWARDS" band and `/app/me/rewards`.
   Badges, certificates and attendance cards stay — they are the recognition layer the audit keeps.

### B · chrome owner — #4 points out of the stats strip (`js/chrome.js`)
1. Line 23: delete `points: 'POINTS', ` from `COPY.stats`.
2. Line ≈100 in `statsStrip()`: delete `${st.quiet ? '' : stat(st.points, COPY.stats.points)}`.
3. `refresh()`: line ≈307 delete `rewards: api.get('/api/rewards/summary'),`; line ≈317 delete
   `points: r.rewards ? Number(r.rewards.balance || 0) : null,`.
   (Audit #5 — hiding remaining zero stats + "MEMBER SINCE 2026" flip — is that owner's call.)

### C · plexus.js owner — #4 the "+10 PTS" chip, #7 Zagreb art (`js/views/plexus.js`)
1. Line ≈72: delete `, pts: '+10 PTS PER CONNECTION'` from `COPY.connect`.
2. Line ≈605: delete the whole chip fragment
   `${(session.user || {}).quiet ? '' : `<span style="padding:2px 7px;…">${COPY.connect.pts}</span>`}`.
3. **Explore Zagreb (#7)** — verdict from the photo sweep: neither `frontend-v2/assets/` nor the
   medx.hr mirror contains a single Zagreb-landmark or food photograph (the mirror's candids are a
   Zürich event; the rest is conference/gala/institutions/portraits). So apply the audit's own
   fallback — text-only reads finished, stripes never do:
   - `zagrebTpl()` › "01 · SIX STOPS BEFORE DINNER": replace the photo-tile `stopCard()` grid with
     numbered text rows using the existing house row vocabulary (Fraunces gold `s.n`, Fraunces
     name, `s.note` in `#4a4239`, hairline `border-bottom:1px solid rgba(25,21,18,.12)` per row);
     KEEP the ink "December bonus: Advent in Zagreb" box as the grid's closing element.
   - "02 · TASTE ZAGREB": drop the 120px stripe strip from each of the four cards (keep the
     name+note text cards — they already look intentional).
   - Hero: remove the stripe underlay + `Z.heroPh` corner label; keep the ink scrim gradient as a
     flat ink hero (it already reads premium with the Fraunces "Dobrodošli u Zagreb.").
   - When real Zagreb/advent photography arrives, recompress ≤300 KB into
     `frontend-v2/assets/` as `zg-<stop>.jpg` and restore the tiles by putting an
     `<img …object-fit:cover>` inside each card exactly as accelerator.js now does its cohort
     blocks (stripe underlay kept as the degrade state).
4. Optional #13 hardening: add class `mx-crumbs` to `crumb()`'s row div — the app.css attribute
   selector already covers it today; the hook makes it drift-proof.

### D · backend owner — #10 Anderson on the Render origin (`user-portal/frontend/assets/gala/`)
`/app/gala` loads speaker images via `api.url()` → the **Render** origin, whose express.static
serves the legacy `user-portal/frontend/`. Copy the fixed square file over the legacy circle-badge
one (same filename):
`cp user-portal/frontend-v2/assets/gala/gala_keynote_anderson.jpg user-portal/frontend/assets/gala/gala_keynote_anderson.jpg`
Longer term the audit asks for one shared speaker-card partial between gala and plexus; their crop
rules are already identical (verified) — a refactor for those owners when convenient.

### E · anyone adding tab strips later — #13
New section-tab strips or breadcrumbs should carry `mx-tabs` / `mx-crumbs`; app.css already
styles those hooks (44px targets at ≤480px), and the `[style*=…]` fallbacks can then be retired.

---

## Files touched by M2

- `js/views/accelerator.js` — cohort photos + fallback binding + `mx-tabs`/`mx-crumbs` hooks
- `css/app.css` — ≤480px 44px tap-target block
- `css/views/gala.css` — five-across stage grid rule
- `assets/ax-cohort-arrival.jpg`, `assets/ax-lab-day.jpg` — new
- `assets/gala/gala_keynote_anderson.jpg` — replaced (square, was circle badge)
- `assets/gala/kn512_{smith_finsbury,delcarmen,spisso,kevin_smith}.jpg`, `mus512_{singer,guitarist}.jpg` — new (copied from legacy frontend)
- `deploy/staging/UXFIX-M2-register-reskin.patch.md` — new (the #3 reskin, ready to apply)
- `deploy/staging/UXFIX-M2-2026-09-02.md` — this report

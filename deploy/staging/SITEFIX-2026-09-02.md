# SITEFIX — 2026-09-02

Implementation of suggestions #1–#9 from `SITE-AUDIT-2026-09-02.md`, applied to the medx.hr deploy-source mirror at
`/Users/alen/Documents/Claude_Code_Projects/MedX_Squarespace/site_live_mirror_2026-07-31/`.
**Nothing deployed, no git, no emails, netlify.toml untouched.** Backups of every edited file (pre-edit state, relative paths preserved) in
`/Users/alen/Documents/Claude_Code_Projects/MedX_Squarespace/_backup_uxsite_2026-09-02/`.

## Files changed (19 edited + 1 created)

| File | Jobs applied |
|---|---|
| `site.js` | 4 — members sign-in bar path gate |
| `donate.html` / `hr/donate.html` | 1, 2, 5 |
| `plexus-sponsor.html` / `hr/plexus-sponsor.html` | 1, 3, 5, 9 |
| `plexus-gala-sponsor.html` / `hr/plexus-gala-sponsor.html` | 1, 3, 5, 6, 9 |
| `building-bridges-sponsor.html` | 1, 3, 5, 9 + language toggle (for 7) |
| **`hr/building-bridges-sponsor.html` — NEW** | 7 (full native-Croatian twin, built from the post-fix EN page) |
| `accelerator-sponsor.html` / `hr/accelerator-sponsor.html` | 1, 2, 3, 5 (page named in audit #1–#3 page lists) |
| `index.html` / `hr/index.html` | 8 (early-bird date, Boston "confirmed shortly") |
| `faq.html` / `hr/faq.html` | 8 (early-bird date) |
| `plexus-gala.html` / `hr/plexus-gala.html` | 8 (countdown fallback date) |
| `building-bridges.html` | 8 (`{E}` → `&ndash;`) |
| `hr/building-bridges.html` | 8 (stale "datum uskoro" lines) + link to new HR twin |
| `hr/get-involved.html` | link to new HR twin (drops "(na engleskom)") |

## What each job did

1. **Real supporter wall.** A compact "In the room with us — Med&X programmes have been backed by" band (HR: "Programe Med&X-a dosad su podržali") with 10 real logos reused verbatim from `/assets/supporters/` (Siemens Healthineers, Ministarstvo znanosti, Središnji državni ured, British Embassy Zagreb, Grad Split, Croatia Airlines, JGL, HLZ, HTZ, ACAP) + "and 15 more — see all supporters" link. Placed in each page's In-good-company section (plexus/bb/accelerator-sponsor), as its own band after the tier grid on gala-sponsor, and under the wizard on donate. The band is static (no `data-medx-list` attribute, so the site.js supporters hydrator ignores it). The founding-partner invitation survives as ONE elegant dark card per page — never a grid of empty slots.
2. **Simulated proof replaced.** Real substitutes transplanted where they exist; deletions listed below. The donate story card now shows the real `cleveland_pjero_i_valentina.webp` photo with a factual caption; the accelerator-sponsor hero names only the placement documented in the mirror (Cleveland Clinic — the hero photo itself is Pjero & Valentina there).
3. **Claims hygiene.** Gala-sponsor hero "100%" stat → wordmark copy "Proceeds / Fund Accelerator fellowships" (HR "Prihod / Financira stipendije Acceleratora"); all MOST POPULAR / MOST CHOSEN / NAJPOPULARNIJE badges removed. The 45/25/15/10/5 allocation bar on donate untouched. Gala tiers keep their audit-endorsed inventory flags (Five available / Three only / One only).
4. **Toast gate.** The bar is injected by `maybeShowPassive()` in `site.js` (~line 754); it fires on any path containing "plexus", i.e. it was hitting /plexus-sponsor and /plexus-gala-sponsor. Added one guard: `if(/-sponsor|\/donate|\/supporters|\/get-involved/.test(path)) return;` — audit #4's exact suppression list, a superset of the four money pages. Verified: suppressed on all five sponsor/donate pages (EN+HR), still appears on /plexus-gala (control), untouched everywhere else.
5. **Bank-transfer rail.** No IBAN exists anywhere in the mirror (grepped; also no financial-page IBAN) — **no IBAN invented**, audit fallback used: "Prefer a bank transfer or an invoice? Write to laura.rodman@medx.hr …" (HR: "Radije biste bankovnu doznaku ili račun? …"). Added: donate wizard final step (`.build-bank`), donate "Registered and accountable" card, and the inquiry-form intro (`.inq-bank`) on all four sponsor pages + HR twins.
6. **Gala-sponsor marquee faces.** The 88px `sg-kn-row` thumbnails replaced with the /plexus-gala confirmed-keynote card module (`gx-kn-*`): large portraits, CONFIRMED KEYNOTE pills, institution logos from `/assets/logos/mono/`, working View-bio toggles (handler script added; HR strings "Pogledaj/Sakrij biografiju"). HR twin uses the ready-translated cards from /hr/plexus-gala.
7. **HR twin created.** `hr/building-bridges-sponsor.html` — full natural-Croatian translation of the fixed EN page (HR nav/footer/form conventions ported from hr/plexus-sponsor; absolute `/assets/` paths per HR convention; canonical + og:url `/hr/building-bridges-sponsor.html`; form option VALUES kept English so the Netlify `support-inquiry` bucket stays consistent, labels Croatian). Language toggle added both directions (EN page had none). `hr/building-bridges.html` and `hr/get-involved.html` links updated to the twin. **_redirects deliberately untouched** — checked first: redirects there are only for HR pages that never existed (heritage, network); real /hr/ files need no entry. **sitemap.xml deliberately untouched** — no other HR sponsor twin is listed there (matched the existing pattern; file was backed up but not modified).
8. **Stale-date/typo sweep.** "early-bird until 1 September [2026]" → "until 15 September 2026" (index, faq, plexus-gala countdown fallback; HR "do 15. rujna 2026."). Boston remnants → confirmed wording: EN index "Exact date confirmed shortly." → "Doors from 5:30 pm; the evening runs 6:00–9:00 pm."; HR index "Točan datum uskoro." → "Ulaz od 17:30, večer traje od 18:00 do 21:00."; hr/building-bridges "Točan datum i vrijeme bit će potvrđeni uskoro." dropped (EN parity) and "Predbilježeni gosti prvi primaju potvrđeni datum." → EN-parity rolling-confirmation line. `{E}` leak in building-bridges.html:487 → `6:00&ndash;9:00 pm`. **Grep-verified zero remnants** of: `{E}`, "1 September" early-bird, "1. rujna" pretprodaja, "confirmed shortly", "datum uskoro", "bit će potvrđeni uskoro", "date to be confirmed".
9. **One primary CTA per hero + narration band deleted.** plexus-sponsor: primary "Become a partner" kept, brochure demoted to quiet underlined text link, "See sponsorship packages" ghost deleted. gala-sponsor: primary "Build your partnership" kept, "Want a custom package?" ghost deleted, brochure demoted (EN+HR). bb-sponsor (+ new HR twin): brochure ghost demoted. HR plexus-sponsor (no brochure): packages link demoted to quiet text link. Black "NOW INTERACTIVELY…"/"Sada interaktivno…" bands deleted on plexus- and gala-sponsor EN+HR ("Step 1 of 3" heading remains the orientation, per audit).

## Deletions (job 2 rule: text that reads finished beats fake proof)

1. `donate.html` + `hr/donate.html` — fictional quote card "Example · a gift, in one life" / "Primjer · dar, u jednom životu" ("Marko, internship 2025" quote) **including its empty monogram portrait placeholder ("M")** → replaced with the real Pjero & Valentina Cleveland Clinic photo figure.
2. `accelerator-sponsor.html` + HR — hero "EXAMPLE"/"PRIMJER" pill, "Illustrative host institutions"/"Ilustrativne ustanove domaćini" note, and the unverifiable Harvard · MGH · Mayo names (Cleveland Clinic kept — documented placements + the hero photo itself).
3. `plexus-sponsor.html` + HR — the empty partner wall's three "Open"/"Otvoreno" slot cells (Gold/Silver/Supporter); founding cell kept as the single invitation card.
4. `building-bridges-sponsor.html` (+ carried into new HR twin) — "Partners to be announced" pill and "Our partners' logos will appear here as agreements are finalized." sentence.
5. `accelerator-sponsor.html` + HR — same "Partners to be announced"/"Partneri će biti objavljeni" pill and "logos will appear here" sentence.
6. `plexus-sponsor.html`, `plexus-gala-sponsor.html` + HR twins — the black "NOW INTERACTIVELY CHOOSE YOUR PARTNERSHIP LEVEL BELOW." narration bands (job 9).
7. Badges: "Most popular" (plexus-sponsor, accelerator-sponsor), "Najpopularnije" (both HR twins), "Most chosen" (bb-sponsor).
8. Hero CTAs: "See sponsorship packages" (plexus-sponsor EN), "Want a custom package?" (gala-sponsor EN+HR).
9. Stale lines: "Exact date confirmed shortly." / "Točan datum uskoro." / "Točan datum i vrijeme bit će potvrđeni uskoro." / "Predbilježeni gosti prvi primaju potvrđeni datum."
10. The "100%" figure (gala-sponsor hero stat, EN+HR).

## Skipped + why

- **IBAN**: none exists in the mirror → fallback copy only (per brief). When a real IBAN is published, swap the `.inq-bank`/`.build-bank`/legal-card lines.
- **network.html fictional directory, home/about empty leadership portraits** (audit #2 side notes): outside the money-page scope set for job 2 — untouched.
- **Audit #10–#12** (merged closes/dead zones, mobile tier accordions, donate ask ladder): out of scope (jobs 1–9 only). Noticed in passing: gala-sponsor's sg-why right-column figure still lazy-loads late (audit #10 territory).
- **accelerator-sponsor hero second CTA** kept (audit #9's page list does not include it; it has no brochure link).
- **sitemap.xml / _redirects**: untouched by design (see job 7).

## Verification

- **HTML parse**: python `html.parser` structural round-trip on all 19 edited/created pages — all OK.
- **Assets**: every `src`/`href` image/css/js/pdf referenced from the 10 reworked pages resolves on disk (absolute `/assets/…` and relative both checked) — none missing.
- **JS**: `node --check site.js` OK; per-page inline scripts intact — Playwright consoles show **0 page errors** on all 5 pages × 2 viewports; `data-inq-tier` preselect, wizard, and form hooks untouched.
- **Toast matrix** (Playwright, fresh storage): donate / plexus-sponsor / gala-sponsor / bb-sponsor / hr-bb-sponsor → no `#medxMembersBar` at 1440 and 390; control /plexus-gala → bar appears. 
- **Rendered + eyeballed** (local static serve of the mirror on :8899 — HR pages and footers use absolute `/assets` paths that raw `file://` cannot resolve): full-page 1440 + 390 screenshots of the four money pages + the new HR twin, plus close-ups of every transplant (band, keynote cards, real-photo proof, bank rails, founding cards, hero CTAs, HR tier ladder). One issue found by looking and fixed: wide logos clipped their plates at 390px → `max-width:min(116px,100%)` on all 10 pages, re-shot clean. Horizontal overflow 0px on all five pages at 390. Screenshots: `/tmp/uxsitefix_shots/`.

Ready for your review and digest-deploy.
